# AI 활용 내역 — 채택 / 수정 / 폐기 기록

규칙: 결정 시점에 즉시 기록 (사후 재구성 금지). 열 = 일시 · 단계 · AI 제안 · 판단 · 이유. 원문 대화는 Claude Code 세션 로그 export (`logs/`) 참조.

| 일시 (KST) | 단계 | 도구 | AI 제안 | 판단 | 이유 |
|---|---|---|---|---|---|
| 08-31 15:0x | 장르 선택 | Claude Code (Fable 5) | 방치형은 적합하나 순수 방치형 대신 "아이들 아케이드(조작 + 자동화 해금)" 권고. 미니게임 1종 트랙을 차선으로 제시 | **채택** | 평가자 3~5분 플레이 제약 + JD "손맛" 키워드. 순수 방치형은 재미 가설 시연이 약함 |
| 08-31 15:1x | 컨셉 후보 | Claude Code + Explore 서브에이전트 | 볼트 아이디어 29건 스크리닝 → 상 10건 → 3안(초식 도장 / 퍼즐용사 / 마다라) + 예비 배틀 레이싱 | **채택 (A안)** | 원본 "모바일 무협"에 수동→숙련도→자동 구조가 이미 있어 변환 손실 최소. B안은 9/2 피벗 fallback 으로 보존 |
| 08-31 15:2x | 원본 미정 항목 결정 | Claude Code | 초식 매칭 규칙: 가위바위보/상성표/시퀀스 길이 중 → **1:1 고정 상성 + 시퀀스 입력** | **채택** | 3~5분 안에 읽기 부담 없이 손맛만 남기려면 판단(상성 추론)보다 실행(시퀀스)에 집중 |
| 08-31 15:2x | 원본 미정 항목 결정 | Claude Code | 진법을 "제자 배치 버프 1종"으로 축소 | **수정 채택** | 원본은 진법 수집·운용이 핵심 재미라 했으나 1주 스코프·1사이클 시연에는 과함 → 1사이클 완료 마커로만 사용, 수집 축은 Scope-out 명시 |
| 08-31 15:2x | 밸런싱 초기값 | Claude Code | 임계 6/8/10, 창 2200/2600/3000ms 등 초기 표 | **잠정 채택** | 근거는 입력 1타 600ms 가정 — 9/3 튜닝 회차에서 실측으로 교체 예정 |
| 08-31 15:2x | 방치 축 시연 | Claude Code | "1시간 수련 시뮬" 버튼으로 오프라인 보상을 즉시 체감 | **채택** | 평가자가 실제로 방치할 수 없으므로 압축 장치 필요 |
| 08-31 16:0x | 검증 | Claude Code + Chrome 자동화 | 헤드리스 하네스(논리) + 실브라우저 사람 속도 봇(페이스) 2단 검증, 결과를 balance-log 0-a/0-b 로 기록 | **채택** | 테스터 모집 전 페이스 기준선 확보. 봇 수치는 사람 실측을 대체하지 않으므로 '참고치'로 표기 |
| 08-31 16:5x | 네이밍 | 사용자 결정 | 게임명 「초식 도장 (Stance Dojo)」 → **「용쟁호투 (龍爭虎鬪)」** 로 변경. 볼트 제안서 폴더·README·PRD·index.html 일괄 반영 | **채택** | 사용자 지시. 위 08-31 15:1x 행의 "초식 도장" 은 당시 후보명 기록으로 보존 |
| 08-31 17:3x~18:3x | 설계 v2 | 사용자 주도 + Claude Code (Fable 5) | 사용자: 사부·제자를 후속으로 미루고 "유저가 직접 고수가 되는 과정" + 후보 필터 시퀀스 입력(자동완성식) + 100% 원터치 + 학습/실전 힌트 분리. Claude: 제자 제거 시 "자동화 해금" 축이 사라지는 문제 지적 → 100% 자동응수 토글로 복구 제안, 발동 규칙에 **포함 금지 규칙**(한 초식 시퀀스가 다른 초식의 완전 접두어 금지) 필요성 제시, 오입력=무시(리셋 아님) 제안 | **채택 (통합)** | 사용자가 "일대종사 3막"(수련→전수→종사)으로 재구성해 제자를 2막으로 복귀시켜 두 요구를 통합. 포함 금지는 4방향 3길이만 64개라 공간 제약 없음을 수치로 확인 후 규칙 채택. 볼트 `일대종사_00_아이디어_v2.md` |
| 08-31 18:0x | 설계 v2 | 사용자 제안 | 실전 슬롯 제한(레벨별 3→8) — 배운 초식은 수십 개라도 장착은 N개, 새 비급을 배우려면 자리를 비워야 함 | **채택** | Claude 평가: 슬롯 제한이 "전수"의 동기가 됨(슬롯 차면 100% 초식을 제자에게 넘겨야 다음 학습). 첫 키 전 후보 접기 UI 규칙도 불필요해짐 |
| 08-31 18:2x | 공방 판정 | Claude Code | 5-3(4단 판정)을 6단으로 확장(열세·역파 추가, 속성 삼각 쾌>강>정>쾌), 선기 +30%, 빈틈 상태, 파라미터 10종 초기값 | **채택** | 4단에서는 "무관 초식 완주 = 안전"이라 후보 필터 화면이 안전 경로를 노출함. 볼트 `일대종사_공방 판정 설계.md` 가 SoT |
| 08-31 18:5x~19:0x | /gamedev-concept R2 | Claude Code 페르소나 CEO/CFO/CTO | `--force` 재평가: 8.0 / 8.0 / 7.75 pass. R1 피드백 12건 중 9건 LLM 자동 수정안 반영(HP회복 광고슬롯→원 환불, rAF 독립 정산, 심사 버퍼, 운영 손익분기 등), 타겟 25~40→18~34, 경쟁 훅 웹 조사(도장 아이들 3종, 숙련 게이트 훅 미발견) | **채택** | 볼트 `일대종사_02_제안.md` / `_02_평가.md`. R2 피드백 중 D1 반영: 판정부 데이터 드리븐 JSON + 하네스 단언 3종 |
| 08-31 18:5x | 엔진 ADR | CTO 페르소나 권고 Unity vs 사용자 결정 | CTO(R1): 모바일 IAA 미디에이션 생태계 근거로 Unity 권고 | **미채택 → HTML5 vanilla 유지** | 사용자 결정. 1인 개발 이식 비용 0, 상용 1차는 웹 포털/PWA, 2차 Capacitor 래핑. CTO(R2)는 웹뷰 입력 지연 실측 미흡수 또는 eCPM 열위 시 Unity 이식을 재결정 트리거로 조건부 재권고 — 제안서 § 9 ADR 에 미채택 사유 기록 |
| 08-31 19:1x | 네이밍 | 사용자 결정 | 게임명 「용쟁호투 (龍爭虎鬪)」 → **「일대종사 (一代宗師)」**, 영문 워킹 타이틀 **Grandmaster Dojo**. 볼트 폴더·노트 9개·README·PRD·index.html·repo 이름(`grandmaster-dojo`) 일괄 반영 | **채택** | 용쟁호투는 대련 한 장면을, 일대종사는 3막 구조의 도착점(플레이어 판타지·문파 개창)을 가리킴. R2 CEO#1 "일대종사가 Tier-1 에겐 단어일 뿐 → Grandmaster 로컬라이즈가 그림" 피드백과 정합. 영문 "Grandmaster" 단독은 체스 ASO 충돌 → Dojo 결합 |
| 08-31 19:0x | 엔진 확정 | Claude Code (Fable 5) | 사용자 질문 "HTML5 vs Unity WebGL 결과물 퀄리티 차이?" → 이 아트 방향(기호·실루엣·궤적)에선 차이가 엔진이 아니라 주스(히트스톱·흔들림·글로우)에서 나고, Unity WebGL 은 첫 로딩 5~15MB·모바일 브라우저 불안정·헤드리스 검증 불가로 과제 조건에 불리하다고 분석. 부족 시 PixiJS 중간 선택지 제시 | **채택 — HTML5 확정** | 사용자 결정. Unity 재검토 트리거에 "아트 방향 격상(스켈레탈 애니·셰이더)" 추가, D4 에 주스 체크리스트 6종 고정. 볼트 `일대종사_엔진 결정 (HTML5 vs Unity WebGL).md` |
| 08-31 19:2x~19:4x | 인프라 | /gamedev-init (Claude Code) | 기존 repo 를 입양하는 모드가 없어 A안 채택: 옛 repo 를 `grandmaster-dojo-archive` 로 개명해 이름 확보 → 같은 이름으로 재생성 → 옛 히스토리 4 커밋 승계(pull) → 볼트 슬롯 `13. 일대종사` (Layout v2 6 폴더) + 제안서·구상 이력 이관 + `2. 프로젝트 소개`(repo_name 앵커) + CLAUDE.md § Vault Reference + 표준 워크플로·하드닝·ruleset·OAuth·glossary 시드. 엔진 필드는 점수 규칙(Godot 6)과 달리 ADR 대로 HTML5 로 기록, variant 부재는 `docs/TODO-workflow-variants.md` | **채택** | 이후 `/gamedev-roadmap·brief·spec·build·check·qa` 가 슬롯 `repo_name` → repo `DesignRoot` 사슬로 위치를 해석하므로 파이프라인(=포트폴리오 기록) 전제. 삭제 대신 개명은 `delete_repo` 스코프 부재 우회 + 원격 히스토리 보존 |
| 09-01 02:2x | 프로토타입 v2 구현 ① | Claude Code (Opus 5, 무인 워커) | v0 의 `index.html` 단일 파일 유지 vs 순수 로직·데이터를 `src/*.mjs` (ESM) 로 분리. 분리안: `index.html` 이 `<script type="module">` 로 로드하고 헤드리스 하네스는 Node 에서 같은 모듈을 import | **채택 (분리)** | 단일 파일로는 하네스가 DOM 없이 순수 함수를 import 할 수 없어 REQ-205·505·506 과 수용 케이스 5·6·8 이 성립 불가. 빌드 단계 0·의존성 0 은 ESM 분리 후에도 유지되므로 엔진 ADR 의 실질(무빌드·무의존)은 보존. 대가는 `file://` 직접 열기 불가 — 변경 전 CLAUDE.md 는 직접 열기도 허용했으므로("Open it directly or serve with…") 무영향이 아니라 실제 축소이고, 그래서 같은 PR 에서 CLAUDE.md 를 로컬 서버 전용으로 정정했다. README 실행법 동기화는 유닛 3 소관 |
| 09-01 02:2x | 프로토타입 v2 구현 ① | Claude Code (Opus 5, 무인 워커) | spec 미지정분 3종을 구현 시점에 확정: ⓐ 도전자 절초 δ 의 파해 대상 = 유운보 (역파의 유일한 발생원) ⓑ 유운검법 2~4식 이름·한자·구결 + 도전자 초식/유파명 ⓒ 빈틈 중 완파 취급은 빈틈을 다시 열지 않음 | **채택** | ⓐ 정(δ)>쾌(유운보) 속성 삼각과 정합 — 「느리지만 읽어 받아친다」가 δ 가 파하는 대상이 쾌 계열이어야 함을 요구한다 ⓑ 조사 노트 §2-5 자연현상 명명 관례를 따름 ⓒ 재개방을 허용하면 연환이 무한히 이어져 전투가 성립하지 않음 (하네스가 이 단정을 pin) |
| 09-01 02:4x | 프로토타입 v2 구현 ① | Claude Code `code-reviewer` (L5) | 제자 자동 선택의 역파 회피 필터가 절초 예고 여부와 무관하게 상시 적용돼, 절초가 안 나온 수에도 α 의 파해 초식(유운보)이 후보에서 빠져 도전자 B 전투 3수가 0피해 상쇄가 된다고 지적. 예고된 초식이 절초일 때만 배제하도록 제안 | **채택 (구현 수정)** | REQ-403 은 「상대 예고가 뜨고」를 트리거로 갖는 event-driven 요구라 그 절의 모든 조건이 그 수의 예고를 기준으로 평가되는 것이 정본 독법이고, 역파는 절초가 실제로 예고된 수에만 성립하므로 그 밖의 배제는 이득 0·완파 손실만 남는다. 수정 후 B 게이트가 8수 HP 정확히 0 → 7수 HP −2·제자 92 로 여유가 늘었다 |
| 09-01 03:1x | 프로토타입 v2 구현 ② | Claude Code (Opus 5, 무인 워커) | 12수 도달 시 잔여 HP 비교를 `core.resolveMatch()` 로 승격하면서 **절대값 비교 vs 비율 비교** 결정 (유닛 ① 인계 사항 1 — spec 미지정) | **채택 (절대값)** | spec 문면이 「잔여 HP 비교」이고, 비율은 최대 HP 가 낮은 도전자를 구조적으로 유리하게 만들어 REQ-506 이 지키려는 첫 파견 승리를 뒤집는다 (제자 100 vs B 80 에서 40 대 35 가 비율로는 패). 하네스가 두 갈래를 모두 단정으로 고정 |
| 09-01 03:2x | 프로토타입 v2 구현 ② | Claude Code (Opus 5, 무인 워커) | 이슈 § 파일 스코프의 조건부 옵션 — `index.html` 이 400~500줄을 넘으면 `src/ui/` 로 화면 단위 분리 | **채택 (분리)** | 4화면 + 입력부 + 대련 루프가 한 파일에서 1400줄대로 계산돼 리뷰 가능 크기를 넘었다. 순수 로직은 유닛 ① 모듈에 그대로 두고 DOM 을 아는 코드만 `src/ui/` 로 내렸다 — `src/` 루트의 DOM-free 계약은 그대로 유지된다 |
| 09-01 03:3x | 프로토타입 v2 구현 ② | Claude Code (Opus 5, 무인 워커) 셀프 플레이 | 헤드리스 사이클 시뮬 중 **막다른 길** 발견: A-3 격파 후 차수가 4로 넘어가 대련 진입이 막히는데, 그 시점에 12성 미달이면 남은 성 성장 경로가 통째로 닫힌다 | **채택 (수정)** | 최고 차수를 클램프해 재진입을 허용 — REQ-503 의 「재진입은 도장에서 수동」과 정합하고, 스펙에 없던 제약을 구현이 만들어낸 자리였다. 함께 채택: 숙련 30% 졸업 시 **빈 슬롯 자동 장착** (선택이 없는 자리라 탭 하나가 kill (a) 60초 예산에 직결) |
| 09-01 03:4x | 프로토타입 v2 구현 ② | Claude Code (Opus 5, 무인 워커) L4 실브라우저 | 응수 창 밖에서 입력 패드를 언마운트하니 매 수(약 1.5s 주기) 레이아웃이 튀는 것이 스크린샷으로 관측됨 | **채택 (수정)** | 패드는 상시 마운트하고 창 밖에서는 흐림 + 입력 비수락으로만 바꿨다 — REQ-101 의 「엄지 존 고정」은 버튼이 매 수 자리를 잃지 않는 것까지가 요건이다 |
| 09-01 04:0x | CI 집행 층 | Claude Code (Opus 5, 무인 워커) | `scripts/comment-lint.py` 의 `.mjs` 확장에서 **정규식 리터럴 판별**을 넣을지 — 넣지 않으면 `/https:\/\//` 의 `\/` 가 줄 주석 시작으로 읽혀 그 줄 나머지를 통째로 주석으로 삼킨다 | **채택 (선행 토큰 휴리스틱)** | 현재 트리에 정규식 리터럴은 0건이지만, 이 lint 는 required check 라 오탐 1건이 이후 모든 PR 을 막는다. 선행 토큰(구두점 집합 + 키워드 집합)으로 나눗셈과 가르고, 줄 안에서 닫히지 않으면 나눗셈으로 되돌리는 보수 폴백을 뒀다. `${...}` 안의 주석은 추출을 포기(오탐 0 쪽으로) |
| 09-01 04:3x | CI 집행 층 | Claude Code `codex` L5d (적대 리뷰) | 3라운드 지적: 「제어문 헤드 판별이 **문자열 안의 괄호**를 구문 괄호로 세므로 `if ("(") /[//]x.py:12/.test(y)` 에서 여전히 오독한다 → 토큰 인식형 파서로 교체하라」 | **미채택 (기각)** | L5d 계약의 기각 트립와이어 (i)(iii) 에 걸린다 — 합성 케이스(문자열 안 불균형 괄호 + 문자 클래스 안 `//` 가 한 줄에 동시 성립)로 휴리스틱을 완전 파서로 격상시키는 발산 요구이고, 변경 전 상태(`.mjs` 집행률 0)보다 이미 보수적이다. 오탐이 실제로 나면 `lint: allow-block(<이유>)` 트레일링 주석이 그 줄만 면제하므로 required check 영구 차단 경로도 없다. 같은 라운드의 medium(diff 실패 fail-open 이 required check 를 green 으로 만든다)은 **채택** — 워크플로에서 리포트 헤더 부재를 실패로 승격 |
| 09-01 05:0x | 프로토타입 v2 구현 ③ | Claude Code (Opus 5, 무인 워커) | 인계 2 — `tests/harness.mjs` 의 `simulateDispatch` 가 `src/ui/match.mjs` 의 교차 루프를 두 번째로 구현한 문제. 대안 ⓐ 하네스 사본 유지 + 대조 단정 추가 ⓑ `createMatch` 에 프레임 시계를 주입하고 가상 시계로 실루프를 돌린다 | **채택 (ⓑ)** | ⓐ 는 사본이 두 벌인 상태를 유지한 채 세 번째 대조군을 늘릴 뿐이다. `timer = {now, schedule, cancel}` 주입은 브라우저 기본값을 그대로 두면서 `match.mjs` 의 전역 의존을 한 곳(`FRAME_TIMER`)으로 모으고, B 밸런스 게이트가 파견 화면과 **같은 코드**를 검증하게 만든다. 결과 동일(7수·제자 HP 92)이라 회귀 없음 |
| 09-01 05:1x | 프로토타입 v2 구현 ③ | Claude Code (Opus 5, 무인 워커) | 봇 v2 가 숙련 100% 초식에서 원터치(REQ-109)를 쓸지. 시도해 보니 창의 78% 가 원터치가 되고 kill (b) 표본이 30건 → 5건으로 붕괴 | **미채택 (봇은 원터치를 쓰지 않는다)** | 원터치 창은 kill (b) 분모에서 제외되므로(REQ-302·603), 탭하는 봇은 자기가 재려던 완주율 표본을 스스로 지운다. 봇의 목적은 페이스 회귀이지 최적 플레이가 아니다. `oneTap` 비율은 보조 지표로 여전히 산출 가능하고 사람 표본이 채운다 |
| 09-01 05:2x | 프로토타입 v2 구현 ③ | Claude Code (Opus 5, 무인 워커) | spec 미지정분 — REQ-605 의 봇 시드는 「15% 미입력」뿐이라 봇이 `ignore`·`reset` 을 한 번도 방출하지 않고, 그러면 REQ-601 의 17종 전량 emit 과 선행 게이트(`ignore_rate`)가 둘 다 공허해진다 | **채택 (`BALANCE.bot.misHitRate` 0.06 신설)** | 「미입력」은 키를 안 누르는 것(→ `timeout`, (b) 분모)이고 「헛손질」은 옆 버튼을 눌러 후보 0이 되는 것(→ `ignore`)이라 **다른 축**이다. 어느 후보의 다음 키도 아닌 방향만 고르므로 반드시 `ignore` 로 남아 계획이 깨지지 않고, 40시드 실측 `ignore_rate` 2.8~11.3% 로 15% 게이트를 실제로 시험한다 |
| 09-01 05:3x | 프로토타입 v2 구현 ③ | Claude Code (Opus 5, 무인 워커) | 판독기(`tests/kill-readout.mjs`)의 종료 코드를 무엇에 걸지 — kill 4항 pass/fail 인가, 판독 가능성인가 | **채택 (판독 가능성)** | kill 미달을 red 로 만들면 CI 가 「밸런스가 아직 안 맞는다」를 빌드 실패로 보고하게 되고, 판정일 전 튜닝 구간이 통째로 red 가 된다. 종료 코드는 **필드 결손·스키마 위반·17종 미방출**만 잡고 (a)(b)(d) 는 출력으로 남긴다 — 수용 케이스 11 이 요구하는 것도 「산출 가능」이지 「임계 통과」가 아니다 |
| 09-01 05:4x | 프로토타입 v2 구현 ③ | Claude Code (Opus 5, 무인 워커) | 세션 로그의 `t_ms` 출처를 `Date.now()` 로 둘지 `performance.now()` 로 바꿀지 | **채택 (단조 시계)** | kill (a)·(d) 는 경과 시간이라 시스템 시계 변경에 흔들리면 안 되고, 대련 루프·힌트 점등이 이미 `performance.now()` 를 쓰므로 로그만 다른 시계를 보면 두 축을 대조할 수 없다. 같은 주입 지점을 헤드리스 봇이 가상 시계로 쓴다 |
| 09-01 05:5x | CI import 해석 검사 | Claude Code (Opus 5, 무인 워커) | named export 파손까지 잡으려면 모듈을 실제로 해석해야 하는데, `src/ui/**` 12종은 최상위에서 DOM 을 만져 Node 에서 `import()` 가 죽는다. 후보 3안 — ⓐ 정규식 정적 파싱 ⓑ `document` 전역 스텁을 깔고 실제 `import()` ⓒ `vm.SourceTextModule.link()` 로 링크만 하고 평가는 생략 | **채택 (ⓒ)** | ⓐ 는 자체 파서를 들여야 하고 주석·문자열에서 오탐이 난다. ⓑ 는 모듈 최상위 부작용(타이머·오디오)이 그대로 돌아 required check 에 무관한 red·행을 만든다 — 차단 게이트의 오탐 1건은 이후 모든 PR 을 막는다. ⓒ 는 V8 의 ResolveExport 가 링크 단계에서 돌아 named export 파손이 잡히고 평가는 `evaluate()` 에서만 일어나 DOM 이 실행되지 않는다(node 22·26 실측). 표준 라이브러리만 쓰므로 의존성 0 계약도 지킨다 |
| 09-01 10:5x | 온보딩 (#15) | Claude Code (Opus 5, 무인 워커) | kill (d) 321.0s 미달의 원인이 조작이 아니라 온보딩 부재(도장 체류 144.2s)로 판독된 뒤, 안내 수단으로 ⓐ 별도 튜토리얼 화면·오버레이 시퀀스 ⓑ 기존 도장 화면 위 상태 기반 툴팁 | **채택 (ⓑ)** | ⓐ 는 1사이클 구조(도장→수련→대련→전수→파견→결과)에 7번째 상태를 붙여 `cycle` 이벤트 스키마와 kill 판독기의 페이즈 가정을 함께 건드린다. 문제는 「화면이 없다」가 아니라 「지금 눌러야 할 게 뭔지 안 보인다」라 표면적이 훨씬 작은 ⓑ 가 원인에 가깝다. 임계(300.0s)·`cycle_done_t` 정의는 손대지 않는다 — 미달 뒤 기준을 옮기는 것은 명시 기각 |
| 09-01 10:5x | 온보딩 (#15) | Claude Code (Opus 5, 무인 워커) | 안내 대상 판정을 ⓐ DOM 결합으로 두고 L4 로 내릴지 ⓑ `session.mjs` 의 순수 함수로 분리해 하네스 단정을 붙일지 (이슈 § 검증 L2 가 갈라 둔 자리) | **채택 (ⓑ 분리)** | 판정 입력이 `disabled` 불리언 목록뿐이라 DOM 없이 성립한다. 분리하면 「최초 진입 1건 · 신규 활성화 고지 · 재고지 안 함」 3축이 회귀망 안으로 들어오고, 실브라우저에서만 확인 가능한 것은 위치·가독성으로 좁혀진다. 대가는 `src/ui/session.mjs` 를 건드리는 것인데 순수 데이터(불리언 목록·id 문자열)만 들어가 DOM-free 계약은 유지된다 |
| 09-01 10:5x | 온보딩 (#15) | Claude Code (Opus 5, 무인 워커) | 안내 1회성의 기준 — ⓐ 버튼을 누른 순간 소비 ⓑ 화면에 띄운 순간 소비 | **채택 (ⓑ)** | ⓐ 는 장착·해제로 잠금이 오가면 같은 말풍선이 반복돼 수용 기준 3 의 「같은 조건으로 다시 뜨지 않는다」를 깬다. ⓑ 로 두되 떠 있는 안내 자체는 대상이 눌리거나 다시 잠길 때까지 유지해, 다른 화면을 한 번 다녀왔다고 유도가 사라지지 않게 했다 |
| 09-01 11:3x | hook 배선 단일화 (#11) | Claude Code (Opus 5, 무인 워커) | 공유 계측 팩토리의 거처 — ⓐ `src/ui/session.mjs` 존치(이슈가 「유력 거처」로 지목, 이미 `logVerdict`·`recordDuelVerdict` 등 공용 로깅 담지자) ⓑ 신규 `src/ui/wiring.mjs` | **채택 (ⓑ 신규 모듈)** | 이슈의 「유력 거처」 근거는 `#15` 랜딩 전 실측이다. 랜딩 후 `session.mjs` 는 277줄·export 34종이고 로깅 + 온보딩 툴팁 상태의 혼합 담지자가 됐다 — 세 번째 축(hook 배선)을 얹으면 「무엇이든 공용이면 여기」가 되어 이 이슈가 고치려는 것과 같은 종류의 응집 상실을 만든다. 별 모듈이면 DOM-free 계약도 파일 단위로 자명해지고, `check-imports` 가 그 간선을 따라 named export 파손까지 본다 |
| 09-01 11:3x | hook 배선 단일화 (#11) | Claude Code (Opus 5, 무인 워커) | 미결 설계 판단 — DOM 렌더 hook 과 계측 hook 의 합성 방식: ⓐ 팩토리가 렌더 콜백을 받아 안에서 부른다 ⓑ 화면이 팩토리가 준 계측 위에 자기 렌더를 얹는다 | **채택 (ⓑ 화면이 얹는다)** | ⓐ 는 렌더 콜백의 타입·수명이 팩토리 시그니처에 새겨져 헤드리스가 쓰지 않는 인자를 계속 넘겨야 하고, 그 압력이 결국 DOM 을 팩토리 안으로 끌어들인다 — 그러면 하네스가 import 하지 못해 공유 자체가 무너진다. `composeHooks(계측, 얹을 것)` 은 같은 이름을 **덮어쓰기가 아니라 연결**로 처리해 화면이 계측 hook 을 조용히 잃는 경로를 구조적으로 없앤다. 계측 반환값(`changes`·`ranked`·실행된 초식)을 두 번째 인자로 넘겨 화면이 재계산하지 않게 했다 |
| 09-01 11:3x | hook 배선 단일화 (#11) | Claude Code (Opus 5, 무인 워커) | 수련 배선을 대련·파견과 같은 `createMatch` hook 형태로 맞출지 | **미채택 (형태를 맞추지 않음)** | 수련에는 `createMatch` 가 없고 창을 여는 지점과 완주 지점 둘뿐이다. `onTelegraph`/`onTick` 을 흉내 내면 아무도 부르지 않는 hook 이 생겨 「hook 이름 집합」 회귀 pin 이 실제 배선이 아니라 형식을 지키게 된다. `onArm`(창 길이를 돌려준다)·`onFire` 2종으로 두고 하네스가 그 집합을 그대로 단정한다 |
| 09-01 13:1x | 화면 고정 (#24) | Claude Code (Opus 5, 무인 워커) | 이슈 § 결정 사항이 축자로 지정한 `--k: min(calc(100vw / 393), calc(100dvh / 852), 1)` 을 그대로 심을지 | **수정 채택 (`min(tan(atan2(100vw, 393px)), tan(atan2(100dvh, 852px)), 1)`)** | 지정 문면은 `min()` 안에 길이 2개와 무단위 수 1개를 섞어 타입이 어긋난다 — 헤드리스 Chrome 152 실측에서 `transform: none` 으로 통째로 떨어졌다(축소 자체가 무발화). `tan(atan2(<길이>, <길이>))` 가 같은 비율을 무단위 수로 돌려주고 상한 1도 같은 `min()` 안에 남는다. 「CSS 만으로 도출」이라는 결정의 실질은 그대로다 |
| 09-01 13:1x | 화면 고정 (#24) | Claude Code (Opus 5, 무인 워커) | 바깥 래퍼의 중앙 정렬을 이슈 문면대로 `place-items: center` 로 둘지 | **수정 채택 (`place-content: center`)** | 스테이지의 레이아웃 상자(852px)가 창보다 클 때 `place-items` 는 그리드 **행 안에서** 정렬하므로 행 자체가 시작점에 붙어 스테이지 아래가 잘린다 — 실측 360x640(Chrome 152)에서 하단 106px 소실. `place-content` 는 행을 컨테이너 안에서 중앙에 놓아 5개 뷰포트 전부 상하·좌우 여백이 같아진다 |
| 09-01 13:1x | 화면 고정 (#24) | Claude Code (Opus 5, 무인 워커) | `#pad` 의 `env(safe-area-inset-bottom)` 처분 — 이슈 § 배경의 원인 4종에 없던 다섯 번째 세로 변동 항 | **채택 (제거)** | 기기마다 값이 달라지는 세로 항이라 「패드 높이 상수」와 직접 충돌한다. 게다가 스테이지가 레터박스 중앙에 놓이면 패드 하단이 더는 기기 가장자리에 접하지 않아 값의 의미 자체가 성립하지 않는다. 대가는 창이 정확히 9:19.5 인 실기기에서 하단 홈 인디케이터와 겹칠 여지이고, 그 확인은 L4 앵커 이슈로 넘긴다 |
| 09-01 13:0x | 툴팁 오버레이 전환 (#20) | Claude Code (Opus 5, 무인 워커) | 오버레이 말풍선의 기준 상자 — ⓐ 앵커(`.tip-anchor`)를 유일한 기준 상자로 두고 좁은 폭 넘침은 `max-width` 상한으로 막는다 ⓑ 가로로 선 줄에서는 `.row-actions` 를 기준 상자로 삼아 줄 전체 폭을 물려받는다 | **채택 (ⓑ)** | ⓐ 는 앵커 폭이 곧 버튼 폭이라(가로 줄의 「수련」은 약 52px) 말풍선이 열 줄로 접히고, 그것을 피하려면 `max-width` 에 앵커의 줄 내 x 오프셋을 CSS 가 모르는 채로 어림한 상수를 박아야 한다. ⓑ 는 `left:0; right:0` 만으로 폭이 정확히 줄 폭이 되어 수용 기준 3(좁은 폭 미넘침)이 상수 없이 성립한다. 앵커는 여전히 흐름 밖 말풍선을 담을 뿐이라 크기 불변 — 대형 버튼 카드 쪽은 앵커 자신이 기준 상자로 남는다. 함께 처분: `#15` 가 넣은 `align-items: flex-start` 2건은 「앵커가 툴팁만큼 커진다」가 전제라 전제 소멸과 함께 삭제 |
| 09-01 19:2x | 도장 세로 예산 (#37) | Claude Code (Opus 5, 무인 워커) | 바닥 밴드의 구현 형태 — ⓐ `#pad` 를 화면마다 다른 내용으로 재사용 ⓑ `#screen` 과 `#pad` 사이에 빈 `#band` 슬롯을 두고 화면이 채운다 | **채택 (ⓑ)** | ⓐ 는 패드가 입력기(`createPad`)의 소유물이고 그 DOM 구조(방향키·후보·버퍼)가 고정이라, 도장 액션을 넣으려면 패드가 화면 종류를 알아야 한다 — 입력 경로 단일화(REQ-101)가 깨진다. ⓑ 는 `#band:empty { display: none }` 로 「채우지 않은 화면에는 자리도 없다」가 CSS 한 줄로 성립하고, `go()` 가 전환마다 비우므로 화면이 남긴 밴드가 다음 화면에 새지 않는다. 함께 처분: `#pad.detached` 를 `visibility: hidden`(272px 상시 점유) → `display: none` — 「전환마다 높이가 튄다」던 예약 사유는 `attach`/`detach` 가 화면 경계에서만 불리는 현재 코드에 존재하지 않는다 |
| 09-01 19:2x | 도장 세로 예산 (#37) | Claude Code (Opus 5, 무인 워커) | 아코디언 기본 펼침 우선순위 — 이슈 § 4 는 「유도 툴팁 대상 행」만 지정. 사용자가 다른 행을 편 뒤 툴팁 대상이 옮겨가면 무엇이 이기는가 | **수정 채택 (사용자 선택 > 툴팁 대상 > 첫 장착 슬롯)** | 툴팁이 항상 이기게 두면 안내가 떠 있는 동안 사용자가 다른 초식의 시퀀스를 볼 수 없다. 이슈의 수용 기준은 「**진입 시** 대상 행이 펼쳐져 있다」이고 진입 시점에는 사용자 선택이 없으므로 두 요구가 충돌하지 않는다. 나머지 수용 기준(대상 버튼이 접힘 아래로 안 내려간다)은 우선순위가 아니라 구조로 지켰다 — 수련·장착·배우기 버튼을 줄 헤더로 올려 접힘 여부와 무관하게 보인다 |
| 09-01 19:3x | 도장 세로 예산 (#37) | Claude Code (Opus 5, 무인 워커) 실브라우저 | `#20` 이 정한 말풍선 기준 상자(`.row-actions`)가 액션 줄 소멸로 사라진 뒤, 후속 상자를 `.row-head` 로 둘지 `.row` 로 둘지 | **채택 (`.row`)** | `.row-head` 는 스크린샷 실측에서 말풍선이 바로 아래 시퀀스 화살표를 통째로 덮었다 — 화살표는 입력 기억 장치라 안내가 그것을 가리면 안내의 대상이 안 보인다. `.row` 는 `#20` 이 만든 배치(말풍선이 줄 아래에 뜬다)를 그대로 잇고 `left:0; right:0` 만으로 폭이 줄 폭이 되는 성질도 유지한다. 밴드 쪽은 화면 최하단이라 반대로 위(`bottom: calc(100% + 4px)`)로 띄웠다 |
