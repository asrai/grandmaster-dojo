---
project: 일대종사
spec: 프로토타입-v2
agenda: 프로토타입 v2 통합 PRD
cycle: 04
previous_cycle: brief_프로토타입_v2_통합_PRD_03
superseded_by_agendas: [안건 1 성 성장 룰 (초식 차등 포인트·3pt/성·11·12성 2배·4배, 승리·파훼 조건 M2), 안건 2 kill (b) 원터치 창 제외, 안건 4 이기는 색 상시 안내 (삼각 줄 폐지·winColorHintExchanges)]
roadmap_milestone: 프로토타입
debate_skipped: TBD
absorbs: brief_후보_필터_시퀀스_입력부_01
created: 2026-09-01 00:49
updated: 2026-09-01 01:17
progress: superseded
superseded_by: brief_프로토타입_v2_통합_PRD_05
tags:
  - 게임개발
  - 문서유형/설계서
---
# 프로토타입 v2 통합 PRD — Brief (cycle 04)

> cycle 03 페르소나 4건(a2·c2)을 안건별 검토(§ 4-1.5-w)로 처분한 재작성본 — 채택 3(운영자 대안 2: 안건 1 **성 성장 룰 = 초식 차등 포인트** · 안건 4 **예고에 「이기는 색」 상시 안내(삼각 줄 폐지)** / 작성자 대응 1: 안건 2 kill (b) 원터치 창 제외) + 폐기 1(안건 3 오터치 지표). cycle 02·03 의 코어 결정(숙련/성 2축 독립 · 순차 해금 · 전수 복사 · 제자 = 자동 용병 · 파견 레인 · 진행형 후보 색 · 4방향 키 통일 · kill 개인별 pass/fail)은 유지. M1 통합 spec 안건(dotfiles #1532). 판정 수식 SoT [[13. 일대종사/1. 구상/공방 판정 설계|공방 판정 설계]], 컨셉 [[13. 일대종사/0. 프로젝트 개요/1. 게임 제안서|1. 게임 제안서]] — 제안서 amend 대상은 § 메타.

## 목적

v0(`index.html` 1.2k 라인, 라운드 1 규칙)를 **v2 규칙의 1막→2막 1사이클**로 교체해, 제안서 §7 kill-criterion 을 **지인 2~4인 + 셀프 플레이로 판정할 수 있는 한 덩어리**를 만든다. 슈퍼센트 과제 제출물(마감 2026-09-07), 판정일 2026-09-02 저녁.

재미 가설은 하나 — **"손으로 익힌 초식이 자동화되는 순간이 보상이다"**. 프로토타입에서 "자동화"는 **제자가 내 무공을 대신 구사하는 것**이고, 그 자격은 내 손이 그 무공을 완벽히 깨달았다(12성)는 증표다. 성립 조건 넷:

- **(a) 입력 = 선택 + 실행** — 선택은 **후보가 갈라지는 분기 키**에서, 그 뒤 꼬리가 실행. 선택의 기준은 **예고에 병기되는 「이기는 색」**(상대 속성에 우세인 1색) — 유저는 그 색의 초식을 찾아 손으로 친다(어느 초식·시퀀스인지는 안내하지 않는다). 같은 비급의 초식은 같은 첫 키로 시작해 도중에 갈라지므로 비급 1권인 프로토타입에서는 2번째 키가 선택 축, 다권(M2+)이면 첫 키부터 비급 단위로 갈라진다(스케일에 따라 분기점이 이동할 뿐 규칙은 같다). **지금 보이는 내 속성 색 = 현재 후보 리스트 최상단 초식의 속성** — 키를 칠수록 색이 답하고, 수동 입력은 도중에 갈래(속성)를 바꿀 수 있지만 원터치는 고정. **입력 장치는 PC·모바일 동일 4방향 키**(PC 방향키/WASD, 모바일 하단 4방향 버튼).
- **(b) 숙련 = 손의 권한, 성 = 깨달음의 증표 (두 축)** — 초식 단위 **숙련도** 0~100%: 힌트(0~30%) → 후보만(30~99%) → **원터치(100%)**. 무공 단위 **성(成)** 1~12: 그 무공 초식의 성공 발동이 **초식 차수만큼 포인트**로 쌓여 오른다(상위 초식일수록 빠름). 성이 오르면 위력 ↑(프로토) · 내공 소모 ↓(M2). 10성이 통상의 최고, 11·12성은 필요 포인트 2배·4배(M2 에서 「해당 무공 승리 N회」·「파훼 N회」 조건 추가, 소급) — **12성 = 전수·분해 자격**. 숙련과 성은 같은 사건(성공 발동)을 먹지만 **독립 축**이다. 시퀀스 입력은 유저 전용(사부 자동응수 없음).
- **(c) 순차 해금 → 12성 → 전수(복사) → 파견** — 하위 초식 100% 가 다음 초식의 학습 조건. 12성 무공은 제자에게 **복사**(사부 보유 유지, 제자는 1성부터). 제자 = 성·레벨이 있는 자동 용병 — **실행은 항상 성공**하고 유저는 어느 제자를 세울지·(원하면) 어느 초식을 쓰게 할지 **지시**만 한다. 2막의 해방 = **첫 파견을 손 놓고 보는 것**.
- **(d) 자명성** — 튜토리얼 없이 첫 초식 발동 ≤ 60초. "예고의 이기는 색 → 내 후보 색 매칭" 과 자동완성식 후보 축소가 설명 없이 읽힌다는 가설.

**판정 형태** (n=2~4 에서 성립하는 것만): kill 4항은 **개인별 pass/fail 의 과반**. (a) 첫 발동 60초 · (c) 설문은 **지인 전용**, (b) 실전 창 손 입력 성공률(원터치 창 제외) · (d) 완주 300초는 지인 + 셀프, **봇은 페이스 회귀 참고치**. 비율 임계는 개인 세션 안 기준. 판정은 기본 설정(접근성 옵션 off). **(c) 문항 = "내가 익힌 무공을 제자가 쓰는 걸 보는 게 좋았다"**(구조의 정서 — 폴백 연출 품질과 분리; 연출 만족도는 보조 문항). **인식률 선행 게이트**: 셀프·봇에서 `ignore_rate` > 15% 면 버튼 히트·배치를 먼저 튜닝한 뒤 (b) 를 읽는다.

**통합 1개로 만드는 이유**: 6 서브시스템이 한 상태기계의 부품(여행자의 단어장 M1 작업 7 — 분할이 통합 결함을 마지막에 노출) + 단일 파일. 대가는 ⑥ 의 서브시스템별 로그 + build 순차 이슈로 상쇄.

## 내용

**MVP 스코프**: 비급 1권 「유운검법」 4식 · 도전자 A(사부 대련, 재도전 3차) + 도전자 B(파견) · 실전 슬롯 3 · 제자 1명(레벨 1 = 무공 슬롯 1) · 6단 판정 · 수련 시뮬 · 로그 내보내기. **범위 밖**: 사부 자동응수·커버(개념 없음) / 스와이프 입력(폐기) / 오프라인 타임스탬프 정산·HMAC 세이브(M2) / 제자 2명+·레벨업·고용(M2) / **제자 능력치 아이템**(사용 시 새 무공을 1성이 아닌 N성부터 — M2+) / 재화 소비처(M2+: 장비 무기·의복 / 소모품 내공 증진·체력 회복 / 비급 구매 / 도장 운영 / 제자 고용·레벨) / 원정 모드(비가시 임무 → 복귀 보고 전투 횟수·전리품 — S1, 오프라인 축과 결합) / 상대 쪽 진행형 색·허초·변초(S2) / 무공 분해·창작·진법(S3) / 광고·IAP / 아트·사운드 실파일(M3 — 코드 렌더·Web Audio 합성 폴백) / 왼손 모드(십자 좌우 배치, M2).

**1사이클 순서와 시간 예산** (목표 300초 — **성공률 70% 가정**, 4식은 사이클 뒤):

| 구간 | 내용 | 성공 필요 | 예산 |
|---|---|---|---|
| 0~35s | 비급 입수 → 유운보 수련 모드 3회 성공 → 30%(장착 가능) | 3 | 첫 발동 ≤ 60s (kill a) |
| 35~90s | 도전자 A 1차(적 초식 α·β) — 유운보 실전 4회 → 100% 원터치 · α 완파 (7pt = 3성) | 4 | |
| 90~145s | 2식 해금·수련 3회 → 30% · A 2차(γ 추가) — 2식 4회 → 100% · β 완파 (21pt = 8성) | 7 | 첫 선택(후보 2, 색 매칭) |
| 145~205s | 3식 해금·수련 3회 · A 3차 — 3식 5회 → 100% · 유운검법 **12성**(45pt, 3식 실전 5회째) | 8 | |
| 205~225s | **전수** — 유운검법 복사(사부·제자 카드에 같은 무공 켜짐 + 12성 배지) | — | 첫 전수 ≤ 225s |
| 225~290s | **파견** — 도전자 B(α·γ + 절초 δ)를 제자가 상대(관전 기본, 선택적 지시) → 등급 「고수」 | — | 완주 ≤ 300s (kill d) |

성공 필요 총 22회(수련 9 + 실전 13). 70% 성공률 → ~31 시도 × 4.3s ≈ 135s + 해금·전환 ~30s + 전수 20s + 파견 12수 ~50s ≈ **235s**. 50% 로 떨어져도 44 시도 ≈ 190 + 100 = 290s — (b) 를 임계로 통과한 사람도 (d) 안에 든다(두 지표 독립). **성 포인트** = 성공 발동(수련 포함)당 초식 차수(1식 1 · 2식 2 · 3식 3) → 최소 경로 7 + 14 + 24 = **45pt**; 2~10성 3pt/성(10성 27pt) · 11성 +6 · 12성 +12 = 45pt → **3식 실전 5회째에 12성**. 대련 중 추가 발동은 앞당길 뿐 늦추지 않는다. 커브(`rankStep`·배수)는 D3·M2 파라미터.

**아키텍처 원칙**: `BALANCE` 단일 객체 + 상태기계 1개(도장 / 사부 대련 / 전수 / 파견) + 초식·무공·도전자·제자 테이블 JSON + 로그 버퍼. 판정·숙련·성·전수·제자 선택은 순수 함수(헤드리스 하네스 DOM 없이). ①~⑥ 경계 = build 순차 이슈 경계.

### ① 후보 필터 시퀀스 입력부 (유저 전용)

- **비급 첫 키 공유·2번째 키 분기**: 유운검법 4식은 모두 `↓` 로 시작(초식 식별자 = 고유 시퀀스, prefix-free; 속성은 초식별 값으로 한 비급 안 혼재). 다권(M2+)이면 비급마다 첫 키가 달라 첫 키부터 갈라진다.
- 장착 = 실전 슬롯 3(장착 조건 숙련 ≥ 30%). 키마다 `candidates = 버퍼를 접두어로 갖는 장착 초식`, **정렬 = 숙련 높은 순 → 동률 슬롯 순**(결정적). **후보 0 이 되는 키 = 오입력 무시**(버퍼·후보 불변). 리셋 버튼(십자 옆 고정 / `Space`·`Esc`)만. **발동 = 후보 1 ∧ 버퍼 == 시퀀스** → `fire{styleId, r}` → ②.
- **진행형 후보 색**: 최상단 초식의 속성이 "지금 내 색"으로 크게 표시(리스트 헤더 + 버퍼 색). 최상단이 바뀌면 색이 바뀐다 — 상대 색(예고 확정 1색)과 대조하며 좁힌다. 후보 1개면 즉시 확정, 원터치는 색 고정. 상대 쪽 진행형 색은 S2 변초의 표현.
- **입력 장치 통일 — 4방향 키**: PC 방향키/WASD, 모바일 **화면 하단 십자 4버튼**(엄지 존, 히트 56px+, 후보 아이콘은 버튼 위쪽 별도 영역) → 같은 `onDirection(dir)`. 스와이프 없음. **입력 피드백 2종**: 수락(버퍼 진행 + 버튼 눌림) / 무시(후보 0 — 셰이크 200ms + 회색 화살표 튕김 + 둔탁음). 같은 창 무시 3회 → 리셋 하이라이트.
- **읽기·판단은 예고, 실행은 창**: 예고 **1.0s**, 창 `T = 2.6 + 0.5 × (len − 3)`(3/4/5 = 2.6/3.1/3.6s — v0 같은-길이 승계 + 긴 초식 여유, D3 는 줄이는 방향). 봇 스펙으로 5길이 2.55s + 분기 판단 0.5s < 3.6s. 접근성 ×1.3 옵션(기본 off).
- 후보 표시: 아이콘(속성 3색 테두리 + 형태 ▲●■). 0~30%: 후보 아래 잔여 화살표 힌트 / 30~99%: 아이콘만 / 100%: 아이콘 탭 = 원터치. 후보 1 도달 시 중앙 확대 + 이름. 버퍼 한 줄(v0 `#seq`).
- `assertPrefixFree(styles[])`: 로드·장착 시 assert(⑤ 와 동일 함수).

### ② 6단 공방 판정부 (데이터 드리븐 + 헤드리스 하네스)

- 한 수: 예고 1.0s(상대 초식 아이콘 + 속성 색 확정 + **이기는 색** + 조짐) → 창 `T` → 교차 판정 → 연출 0.5s. 대련 = HP 소진 또는 12수.
- 판정표(판정 설계 §2-2): 파해 → 완파(100% + 상대 빈틈) / 속성 승 → 우세(60/20) / 동속성 → 상쇄(내공차 × k) / 속성 패 → 열세(20/60) / 상대 절초의 파해 대상을 냄 → 역파(0/100 + 나 빈틈) / 미완주 → 피격. 쾌>강>정>쾌, 파해 우선.
- 수식: `D` 10/14/20 · **내공 `N = 1 + 0.05 × 성`**(판정 설계 §3 의 숙련 기반 `N` 대체; 10성 1.5, 12성 1.6. 내공 소모 자원과 성에 따른 소모 감소는 M2) · 선기 `1 + 0.3r` · 상쇄 `max(0, ΔN) × D × k`(0.5) · 빈틈 1수.
- 전부 `BALANCE` JSON, 순수 함수 `judge()` 로 6단 × 빈틈 × 선기 × 성 전 조합 하네스 회귀. 판정표 변경 = 하네스 기대값 같은 커밋.
- 정보 노출: 상대 속성 색 항상 + **「이기는 색」 병기**(예고 옆 `→ ●청` — 상대 속성에 우세인 1색; `winColorHintExchanges` = ∞ 프로토 상시, 페이드 시점은 D3/M2 파라미터) / 내 진행형 색 항상 / 파해는 구결 텍스트만 / 절초 1개는 도전자 예고 / 잔여 힌트 0~30% 만. **우세 *후보 아이콘* 강조(펄스) 없음** — 속성은 안내하되 어느 초식·시퀀스인지는 유저가 찾아 친다. 삼각 규칙표 한 줄은 두지 않는다(이기는 색이 대체).

### ③ 숙련(초식) · 성(무공) · 순차 해금 · 실전 슬롯 · 전수

- **숙련도**(초식 0~100%): 수련 모드 **3회** 성공 → 30%(졸업·장착 가능) / 실전 성공(판정 ≥ 상쇄) 누적 `threshold`(**유운보 4 · 2식 4 · 3식 5 · 4식 5**) → 100% = 원터치. 슬롯에서 빼도 보존.
- **순차 해금**: N식 숙련 100% → N+1식 학습 가능(도장 「배우기」). 재화 게이트 없음.
- **성**(무공 1~12): **성 포인트** `rankPts` = 그 무공 초식의 성공 발동(수련 모드 포함, 판정 ≥ 상쇄)당 **초식 차수**(1식 1 · 2식 2 · 3식 3 · 4식 4). 2~10성 = 누적 `rankStep` 3pt/성(10성 27pt) · **11성 = +6(2배) · 12성 = +12(4배)** = 45pt. **M2**: 11성에 「해당 무공으로 승리 N회」, 12성에 「해당 무공으로 파훼 N회」 조건 추가(소급 적용) — 프로토는 포인트만. 성 → 위력 `N`(+ M2 내공 소모 감소). 성 상승 = 무공 카드 「N성」 배지 + 짧은 연출.
- **실전 슬롯 3**: 4식이 열리면 4 > 3 이라 장착 선택 발생(도장에서만 교체).
- **전수 = 복사**: 조건 = 무공 12성 ∧ 제자 무공 슬롯 여유. 사부는 무공·숙련·성 보유. **제자 무공은 1성부터**(M2+ 아이템으로 시작 성 상향). **전수 장면 = 구조적 순간**: 「유운검법 12성 — 완벽히 깨달음」 배지 → 사부 카드·제자 카드가 나란히, 같은 무공 아이콘이 제자 쪽에 **켜진다** → (파견 첫 수에서 제자가 그 초식을 친다). 시연 1회·스팅어·1.0s 연출은 덧칠(폴백 합성음). 12성 무공 분해는 표시만(S3).
- 단계 라벨: 문하생 → 고수(첫 전수). 사부·일대종사는 M2+.

### ④ 제자 = 자동 용병 · 파견 (관전 기본 + 선택적 지시)

- 제자 1명(레벨 1 · 무공 슬롯 1). 제자 무공 성 1~10(같은 성 포인트 룰, 상한 10 — 12성은 유저만). **제자 실행은 항상 성공**(실패·성공률 없음) — 성은 위력 `N` 만. 실행 시점 = 창의 **60%**.
- **파견 = 관전이 기본**: 도전자 B 와 제자가 대련, 제자는 매 수 **자동 선택**(상대 속성에 우세인 초식 중 성 높은 것; 없으면 상쇄; **상대 절초의 파해 대상 초식은 제외**(역파 회피)). 유저 화면은 보는 화면 — 알림·재촉·타이머 없음.
- **지시 = 원할 때만**: 예고 중 제자 초식 아이콘이 작게 떠 있고, 탭하면 그 수만 지정(파해 노림 → 완파·연환). 탭 여유 = 예고 1.0s + 창의 60% 시점까지 ≈ 2.5s. 파해로 끝낼 수 있는 수(도전자 예고에서 공개된 정보 기준)는 아이콘 **반짝임 1회**로 유도(강제 아님).
- 제자 패배 = 도전자 도주(손실 없음). 파견 결과 화면: 승패 · 성 변화 · 재화. 원정(비가시·보고형)은 S1.

### ⑤ 데이터 — 유운검법 4식 · 도전자 A/B · 도전자 예고

- 초식 테이블: id · 무공 · 이름 · 한자 · 속성 · 시퀀스(첫 키 `↓`, 2번째 키 분기) · `D` · 파해 대상 · 구결. 4식 = 유운보 쾌 3(`↓→↑`) · 2식 강 3(`↓←→`) · 3식 정 4(`↓↑→←`) · 4식 쾌 5(`↓↓→←↑`) — 속성 혼재라 분기 키 = 속성 선택.
- 무공 테이블: 유운검법 {초식 4, 성 1~12, 전수 조건}. 제자 테이블: {레벨 1, 무공 슬롯 1, 성 map}.
- 도전자 A(사부, 재도전 3차: α → α·β → α·β·γ, 절초 없음; α 파해 = 유운보, β = 2식, γ = 3식) / 도전자 B(파견: α·γ + 절초 δ 공개, δ 파해 = 4식 — 사이클엔 없어 제자는 3속성 중 우세로 깎되 완파는 못 함 → 4식 동기). **B 밸런스 게이트**: 1성 제자(N 1.05) 무지시 자동으로 B 를 이기는지 하네스 시뮬(12수·HP) — 못 이기면 B 의 `N`·HP 를 낮춘다(kill d 는 승패 무관 완주 판정이지만 첫 파견 패배는 해방 정서를 죽인다).
- **도전자 예고 화면**(파견 전): 유파명 + 절초 δ 아이콘·속성 → 제자 세팅 확인. 파해 1:1 무결성 + `assertPrefixFree` 를 데이터 커밋마다 하네스. 이름·구결 [[무공 초식 조사]] §2-5 수작업 4+4.

### ⑥ 검증 로그 · 재화 · 봇 v2 · 파라미터 표

- 로그(세션 버퍼 → 「로그 내보내기」 JSON): `key{dir, accepted, candidates_n, top_attr, device: keyboard|button}` · `ignore{dir}` · `reset` · `narrow{styleId}` · `fire{styleId, len, oneTap, r}` · `timeout` · `verdict{grade, dmg_out, dmg_in, state, who}` · `mastery{styleId, from, to}` · `rank{style_set, from, to, pts}` · `unlock{styleId}` · `slot{action, styleId}` · `transmit{style_set}` · `dispatch{challenger}` · `select{styleId, byUser}`(지시/자동) · `coins{delta, reason}` · `cycle{phase}` · `session{tester_role: self|friend|bot, device}`. 전부 `t_ms`.
- **개인별 pass/fail**: (a) friend `first_fire_t ≤ 60s` / (b) self+friend **실전 창 중 원터치 창 제외** `fire(oneTap=false) / (fire(oneTap=false) + timeout) ≥ 0.5`(장착 = 숙련 ≥ 30% 라 실전엔 힌트 없음; 표본 개인당 n ≈ 15~25, 35~205s) / (c) friend 설문 "내가 익힌 무공을 제자가 쓰는 걸 보는 게 좋았다" ≥ 4 / (d) self+friend `cycle_done_t ≤ 300s`(= `dispatch` 종료). **선행 게이트**: `ignore_rate` > 15%(셀프·봇) → 입력 튜닝 후 (b) 판독. 보조: `first_transmit_t` · `select.byUser` 비율 · `oneTap` 비율 · `ignore_rate`(device 별) · `tail_ms` · `top_attr` 전환 횟수 · 연출 만족도 문항.
- **재화**: 획득·표시만(수련 시뮬 `simEfficiency` 0.1 × 3600 + 대련·파견 보상). 소비처 M2+(장비·소모품·비급·도장 운영·제자 고용·원정 전리품).
- 봇 v2: 반응 0.45~0.65 · 키당 0.26~0.38 · 15% 미입력으로 1사이클 자동(파견 무지시) → `balance-log.md`. `tester_role: bot`.
- `BALANCE`: 예고 1.0 / `T` 2.6 + 0.5·(len−3) / 접근성 1.3(off) / `D` 10·14·20 / 내공 0.05·성 / 우세·열세 60·20 / k 0.5 / 선기 0.3 / 빈틈 −40% / 수 상한 12 / 수련 졸업 3 / `threshold` 4·4·5·5 / `rankPtsPerStyle` 1·2·3·4 / `rankStep` 3 / `rank11Mult` 2 / `rank12Mult` 4 / 제자 시작 성 1 / 제자 실행 0.60 / `winColorHintExchanges` ∞(프로토 상시) / `simEfficiency` 0.1 / 버튼 히트 56px.

## 예상되는 플레이와 기대하는 재미

- **0~60초 (긴장)**: 비급을 열면 유운보(청). 도전자 예고에 적(강) + 이기는 색 ●청 → 장착이 유운보(청)뿐이라 첫 키에 후보 1, 내 색 청 = 이기는 색 확인 → 힌트 화살표 따라 하단 버튼(모바일)/방향키(PC) 세 번. "색 확인 → 따라 치기" 두 걸음. α 를 유운보로 파하면 「완파!」 + 빈틈 → 연환.
- **1~3분 (숙달·선택)**: 30% 에 힌트가 사라지고 꼬리는 손이 친다. 유운보 100% 원터치 → 선기 +30%. 2식(강)이 들어오는 순간 `↓` 뒤 **후보 둘, 내 색 = 최상단(유운보·청)**. 예고에 녹(정) + 이기는 색 ●적 — 내 색이 청이니 `←` 로 갈래를 바꿔 적에 맞춘다. 첫 선택은 대조가 아니라 **색 매칭**. 3식(정)이 오면 `↑` 로 갈라지며 색이 녹으로 바뀐다 — "입력할수록 화면이 답한다". 상위 초식일수록 성이 빨리 올라 3식을 손에 익히는 사이 **12성**(3식 실전 5회째).
- **3.5분 (전수)**: 12성 배지 → 사부 카드 옆 제자 카드에 유운검법이 **켜진다**. 무공은 내게 남고 제자에게도 생겼다.
- **4~5분 (해방·파견)**: 도전자 B 예고에 절초 δ(정). 제자를 보내고 **손을 놓는다** — 제자가 색을 보고 알아서 친다. 파해로 끝낼 수 있는 수에 아이콘이 한 번 반짝이면, 원하면 탭해서 "그걸로 끝내" 라고 지시한다. 이기면 「고수」. "4식을 배우면 δ 도 완파" 가 다음 동기.
- **실수 경험**: 오입력 무벌(셰이크), 갈래 오진입도 유효 응수(색이 바뀌는 걸 보고 알아챔). 스와이프가 없어 "안 읽혔다" 는 없다.
- **검증 로그 → 개인별 pass/fail**: (a)(c) friend / (b)(d) self+friend — 4항 각각 과반 fail 이면 kill(제안서 §7 → B안 「퍼즐용사」 피벗). `ignore_rate` 선행 게이트. 보조 지표는 D3 튜닝 입력.

## 예상되는 문제점과 해결 방안

1. **새 초식이 들어와 후보가 둘·셋이 되는 첫 선택에서 멈춘다**.
   → 해결: **예고가 이기는 색을 알려주고**, 유저는 그 색의 초식을 손으로 친다 — 첫 선택은 색 대조가 아니라 **색 매칭**. 진행형 색이 "지금 가는 갈래의 색"을 보여줘 이기는 색과 맞는지 즉시 확인. 순차 해금이 후보를 1→2→3 으로 키워 매칭 대상이 한 번에 하나씩 는다. 우세 *후보 아이콘* 직접 강조는 없음(어느 초식인지는 유저가 찾는다). 노출 기간 `winColorHintExchanges`(프로토 상시).

2. **오입력이 "안 눌린다"로 읽힌다** — 후보 0 이 되는 키를 눌렀을 때.
   → 해결: 수락(버튼 눌림 + 버퍼 진행) / 무시(셰이크 + 회색 화살표 튕김 + 둔탁음) 2종 피드백 + 같은 창 무시 3회 → 리셋 하이라이트. 모바일은 하단 십자 버튼이라 인식 실패 자체가 없음. `ignore_rate` 가 15% 를 넘으면 버튼 히트·간격을 먼저 튜닝.

3. **잘못 든 갈래로 좁혀졌는데 모른 채 완주**.
   → 해결: 갈래를 잘못 들면 **내 색이 바뀌어 이기는 색과 어긋난 것이 보인다** + 후보 1 도달 시 확대·이름 / 장착 조건 ≥ 30% 라 잔여 시퀀스를 안다 / 완주하면 유효한 응수. 리셋은 포기 버튼.

4. **4·5길이 창이 빡빡해 리듬게임으로 읽힌다**.
   → 해결: 판단은 예고 1.0s, 창은 실행 전용 + 긴 초식 계수 0.5(넉넉히 시작해 D3 에서 줄임). 접근성 ×1.3 옵션.

5. **파견에서 "지시할 이유가 안 보인다"** — 관전이 기본이라 지시 기능이 있는지도 모르고 지나간다.
   → 해결: 파해로 끝낼 수 있는 수(도전자 예고 공개 정보 기준)에 제자 초식 아이콘 **반짝임 1회** — 지시하면 완파·연환이 되는 것을 한 번 보여주는 유도, 강제 아님. `select.byUser` 로 개입도 측정. 개입 없이도 제자가 이기는 것이 첫 파견의 본체.

6. **12성까지의 반복이 지루하다**.
   → 해결: 성은 3pt 마다 배지 연출이고 **상위 초식일수록 포인트가 커** 후반이 가속된다(3식 = 1식의 3배). 11·12성의 승리·파훼 조건은 M2. 순차 해금이 초식을 하나씩 열어 후보·색 변화가 계속 새로움. 성공 필요 22회로 압축. 커브는 D3·M2 파라미터.

7. **전수가 밋밋하다** — 연출 리소스가 폴백뿐.
   → 해결: 전수의 실체를 연출이 아닌 **구조**로: 12성 배지 → 사부·제자 카드에 같은 무공이 켜짐 → 파견 첫 수에서 제자가 그 초식을 친다. 코드 렌더로 성립. kill (c) 문항도 구조의 정서를 묻는다.

8. **6단 판정 결과가 한 수 안에 읽히지 않는다**.
   → 해결: 등급 = 색 + 한 글자 즉시 중앙 / 피해 = HP 바 애니메이션 / 빈틈 = 다음 예고 자리 「빈틈!」 배너. 접근성 색 + 글자 + 형태.

## 논의 안건

**(안건별 검토 완료 — 05 cycle 로 이월: 안건 1~4 전건 brief 재작성 대상. 결정 상세 = § 메타 「cycle 04 안건별 운영자 결정」)**

### brief 보강 풀 (a 일관성 — 작성자 재작성 대상)

- **안건 1** — 도전자 A 재도전 차수별 적 초식 구성이 예산표와 데이터 섹션에서 어긋난다
  - 심각도: important
  - 출처: (a) 일관성 — § 내용 1사이클 시간 예산표 ↔ § 내용 ⑤ 데이터(도전자 A)
  - 근거: 예산표는 「35~90s A 1차(적 초식 α·β)」 / 「90~145s A 2차(γ 추가)」 로 적고, ⑤ 는 「재도전 3차: α → α·β → α·β·γ」 로 적어 한 차수씩 밀려 있다. ⑤ 의 파해 매핑(α=유운보, β=2식, γ=3식)과 붙어서 단순 표기가 아니다 — 예산표 해석대로면 1차에서 아직 해금 안 된 2식의 대상(β)을 유운보로 받게 되고, ③ 의 실전 성공 정의(판정 ≥ 상쇄)상 그 수가 숙련·성 카운트에 드는지가 β 속성에 따라 갈린다. 「성공 필요 4회」·「12수 상한」·「22회 총량」 산식의 근거가 어느 해석이냐로 달라진다.
  - 토의 유형: 일관성 결함 해소
  - 권장 처리: brief 보강 (작성자 재작성)
  - 처분: 05 이월 — 대응 채택(표를 ⑤ 기준 정정)

- **안건 2** — "성공" 이 세 곳에서 서로 다른 사건을 가리켜 (b)↔(d) 독립 주장과 인식률 선행 게이트가 같이 흔들린다
  - 심각도: important
  - 출처: (a) 일관성 — § 목적 판정 형태 ↔ § 내용 시간 예산 ↔ § 내용 ⑥ 로그·지표 정의
  - 근거: ⑥ 의 (b) 는 `fire(oneTap=false)/(fire+timeout)` = 창 안에서 무언가를 완주했는가 이고, ③ 의 숙련·성 진행은 판정 ≥ 상쇄 만 먹는다. § 플레이의 「갈래 오진입도 유효 응수」 명시상 속성이 어긋난 완주(열세)는 (b) 분자에는 들고 22회 진행에는 안 든다. 그런데 예산 문단은 두 값을 같은 "성공률" 로 두고 「(b) 임계 통과자는 (d) 안에 든다(두 지표 독립)」 를 도출한다. 같은 결로 `ignore_rate` 게이트는 후보 0 키만 세므로 인접 오터치(유효 갈래로 튐)는 게이트에 안 잡히고 (b) 에 성공으로 잡힌다.
  - 토의 유형: 일관성 결함 해소
  - 권장 처리: brief 보강 (작성자 재작성)
  - 처분: 05 이월 — 대응 채택(완주율/유효 성공 분리)

### debate 안건 풀 (b/c — 페르소나 토의 입력)

- **안건 3** — 문제점 4 의 해결 근거인 "창은 실행 전용" 이 본문의 분기 설계·창 예산과 충돌한다
  - 심각도: important
  - 출처: (c) 해결책 타당성 — 문제점 4 해결책(판단은 예고 1.0s, 창은 실행 전용 + 긴 초식 계수 0.5)
  - 근거: § 목적 (a) 는 선택 축을 2번째 키에 두고, ① 은 「키를 칠수록 색이 답하고 … 대조하며 좁힌다」, 문제점 1 해결책은 「진행형 색 … 즉시 확인」 — 판단이 창 안에서 일어난다. ① 의 창 예산 자체가 「5길이 2.55s + 분기 판단 0.5s < 3.6s」 로 in-window 판단을 명시 계상한다 — "실행 전용" 전제가 브리프 자신의 타이밍 모델과 다르다. 사이클 최장 3식(4키, T=3.1s)에서 봇 최악치 여유 0.4s대, 초심자 키 간격은 봇 스펙보다 느린 쪽. 가정이 깨지면 문제점 4 의 반대(타임아웃 급증)가 나오고 D3 「줄이는 방향」 이 리스크를 키운다.
  - 토의 유형: 해결책 합리성 검증
  - 권장 처리: debate 안건
  - 처분: 05 이월 — 대응 채택 + 「상대 예고」 명명 통일

- **안건 4** — 문제점 3 의 "≥30% 면 잔여 시퀀스를 안다" 가 수련 3회 = 암기 완료 가정에 통째로 얹혀 있다
  - 심각도: important
  - 출처: (c) 해결책 타당성 — 문제점 3 해결책(장착 조건 ≥ 30% 라 잔여 시퀀스를 안다)
  - 근거: 30% 경계에서 잔여 화살표 힌트가 절벽처럼 끊기고, 그 직후 구간(35~205s)이 곧 kill (b) 표본 구간이다. 3식 4키·4식 5키를 수련 3회 반복으로 손에 남긴다는 가정은 시퀀스 학습 경험상 낮게 잡은 쪽 — 깨지면 30% 직후 타임아웃이 몰려 (b)·(d) 가 동시에 내려가는데, 그 실패가 재미 가설의 반증인지 수련 졸업 파라미터(3회) 문제인지 판정에서 분리되지 않는다. 해결책 3 의 나머지 두 축(색 어긋남 가시 / 후보 1 확대)은 "잘못 든 것을 안다" 의 재진술이라 "잔여 키를 칠 수 있다" 축을 덮지 못한다.
  - 토의 유형: 해결책 합리성 검증
  - 권장 처리: debate 안건
  - 처분: 05 이월 — 운영자 대안(딜레이드 힌트)

---

## 원 답변 로그

| 섹션 | 처리 모드 | 내용 요약 |
|---|---|---|
| 목적 | 수락 (cycle 04 재작성) | 이기는 색 = 선택 기준 · 성 = 초식 차등 포인트(11·12성 조건 M2) · kill (b) 손 입력 한정 |
| 내용 | 수락 (cycle 04 재작성) | 표 45pt = 12성(205s) · ② 이기는 색 상시(삼각 폐지) · ③ rankPts 1·2·3 / 3pt·+6·+12 · ⑤ δ 우세로 깎음 · ⑥ (b) 원터치 제외 · BALANCE 갱신 |
| 예상되는 플레이와 기대하는 재미 | 수락 (cycle 04 재작성) | 첫 선택 = 색 매칭 · 12성 = 3식 5회째 |
| 예상되는 문제점과 해결 방안 | 수락 (cycle 04 재작성) | 8건 (1 이기는 색 매칭 · 3 어긋남 가시 · 6 상위 초식 가속) |
| 논의 안건 | 안건별 검토 4/4 완료 → 05 이월 | 채택 4 / 폐기 0 |
| 참고 자료 | 없음 (승계) | - |

## 메타
- 커맨드: `/gamedev-brief`
- 로드맵 작업: [[13. 일대종사/3. 로드맵/로드맵 현황|로드맵 현황]] / 프로토타입 / 작업 #1
- 생성 경로: `docs/design/1.01-프로토타입-v2/brief_프로토타입_v2_통합_PRD_04.md` (브랜치 `design/1.01-프로토타입-v2`, worktree `../grandmaster-dojo-design`)
- cycle 02 → 03 안건별 운영자 결정 (§ 4-1.5-w, 2026-08-31 23:0x):
  - 안건 1 → 대응 채택(파라미터): 수련 3 · `threshold` 4·4·5·5 · 성 2회당 · 예산 70% 가정(235s / 50% 시 290s) — (b)·(d) 독립.
  - 안건 2 → 대응 채택(서술 정정+파라미터): 삼각 한 줄 = 해금마다 첫 5수.
  - 안건 3 → **운영자 B안**: 제자 복사 1성 시작 · **실행 실패 없음(`p` 삭제)** · 아이템 시작 성 상향은 M2+.
  - 안건 4 → 대응 채택(해결책 교체): 파견 관전 기본 + 선택적 지시(탭 여유 ≈2.5s, 제자 실행 60%), 문제점 5 재정의.
  - 안건 5 → 대응 채택: kill (c) 문항 교체 + 구조적 전수 장면.
  - 안건 6 → **운영자 대안(UI)**: 스와이프 폐기, 모바일 하단 4방향 버튼으로 PC 와 통일. 미인식 개념 소멸.
- **제안서 amend 대상**(spec 확정 시 `↩ brief 03`): §5-2 숙련 4단계 → 3단계 + 성 축 / §5-3 전수 = 슬롯 비우기 → 12성 복사 / §5-5 분할 화면·커버 → 파견·지시 / §8 조작 "방향키/WASD/스와이프 3중" → 4방향 키 통일(하단 버튼) / §9 QA 봇 = 참고치 / §10 재화 소비처 후보 / §7 kill (c) 문항 · (b) 손 입력 한정 / **§5-2 성 성장 룰**(초식 차등 포인트 · 11·12성 승리·파훼 조건 M2 · 내공 소모 감소 M2) / **§6 정보 노출 「이기는 색」**(↩ brief 04).
- 용어 사전 append 후보(§ 4-2): 성(成) · 성 포인트 · 순차 해금 · 진행형 후보 색 · 이기는 색 · 파견 · 지시 · 원정 / 별칭: 커버 → (폐기) · 자동응수(사부) → (폐기) · 스와이프 입력 → 4방향 버튼 · 삼각 힌트 → 이기는 색.
- cycle 04 안건별 운영자 결정 (§ 4-1.5-w):
  - 안건 1 → 대응 채택(서술 정정): 표를 ⑤ 기준으로 — A 1차 = α만 / 2차 = α·β(β 추가, 첫 선택·β 완파) / 3차 = α·β·γ(γ 추가). 예산·22회 산식 불변.
  - 안건 2 → 대응 채택(서술 정정 — 용어 분리): **완주율**(손 입력 fire, 판정 무관, 원터치 제외 = kill (b)) vs **유효 성공**(판정 ≥ 상쇄 = 숙련·성 적립). 예산 "성공률 70%/50%" → "유효 성공률" 라벨 정정(산식 불변) · 독립 문장 재서술 · "오진입 완주 = (b) 성공·적립 미포함" 의도 명시. 후반부 인접 오터치 게이트 지적은 cycle 03 폐기 결정 유지로 제외.
  - 안건 3 → 대응 채택(서술 정정 + 파라미터 가드) + **「상대 예고」 명명 통일**: "창은 실행 전용" → "판단 대부분은 상대 예고에서 끝나고(이기는 색 선독) 창 안은 2번째 키 갈래 확인 1회 + 실행(예산에 0.5s 계상)". D3 창 감축은 `timeout`·`tail_ms` 데이터 게이트 뒤에서만. 초심자 검산 1줄(3식 4키 × 0.5s + 0.5s = 2.5s < 3.1s). 예고 = 상대(도전자) 텔레그래프임을 명명으로 문면화 — 유저 쪽 색은 진행형 후보 색으로 구분(운영자 오독 사례에서 확정).
  - 안건 4 → **운영자 대안(규칙 변경 — 딜레이드 힌트)**: 힌트 구간 규칙(0~30% 만) 삭제, 미완성(<100%) 초식 잔여 화살표를 키마다 `hintDelay` 0.5s 뒤 점등(실전; 수련은 즉시 0) — 힌트 의존 완주 3키 ≈2.55s ≤ T 2.6s 성립 / 4키 3.4s·5키 4.25s > T 탈락 → 긴 초식은 자연 암기 유도, 30% 절벽·"암기 완료 가정" 소멸. 30% 는 장착(졸업) 조건으로만. `timeout{styleTop, buffer_len}` 로그 보강. (작성자 A안 「졸업 시험」·B안 「판독 룰」 은 운영자 대안으로 대체.)
  - 종합: 채택 4/4(안건 1·2 서술 정정 · 3 서술 정정+명명 · 4 운영자 규칙 변경) / 폐기 0 → NN+1(05) 재작성 1회
- cycle 03 → 04 안건별 운영자 결정 (§ 4-1.5-w, 2026-09-01 00:1x~00:4x):
  - 안건 1 → **운영자 대안(규칙 변경 — 성 성장 룰 교체)**: 성 포인트 = 성공 발동(수련 포함)당 초식 차수(1식 1 · 2식 2 · 3식 3 · 4식 4) · 2~10성 3pt/성(10성 27pt) · 11성 +6(2배) · 12성 +12(4배) = 45pt → 표 최소 경로(7+14+24 = 45pt)로 3식 실전 5회째 = 12성(205s) 정합. 11성 「해당 무공 승리 N회」·12성 「해당 무공 파훼 N회」 조건은 **M2 로 연기(소급 적용)**, 프로토는 포인트만. 성 효과 = 위력 `N`(프로토) + 내공 소모 감소(M2). 연쇄: § 목적 (b) 완파 조건 → M2 / 문제점 6 해결책 → 상위 초식 가중치 가속 / 플레이 "완파 두 번에 12성" → "3식 익히는 사이 12성" / `BALANCE` `rankPtsPerStyle` 1·2·3·4 · `rankStep` 3 · `rank11Mult` 2 · `rank12Mult` 4 (「성 2회당 1」「11·12성 완파 1·2」 삭제) / 제자 성 동일 룰(상한 10). (중간 제안 「숙련 10% = 1성 연동」은 운영자 철회 — 두 축 독립 유지.)
  - 안건 2 → 대응 채택(서술 정정): kill (b) 모집단 = 실전 창 중 원터치 창 제외 — `fire(oneTap=false) / (fire(oneTap=false) + timeout) ≥ 0.5`. "힌트 소멸 후" → "실전 창(장착 = 숙련 ≥ 30% 라 힌트 없음)" 정정. 표본 개인당 n ≈ 15~25(35~205s). `oneTap` 비율은 보조 유지.
  - 안건 3 → **폐기(운영자)**: 개별 수의 "오터치 vs 의도" 는 관측 불가 — 기획 결정 대상이 아니라 D3 튜닝 편의. 문제점 2 문면 유지. (장치 간 차 proxy 로그는 원하면 spec/D3 에서 필드 추가 가능 — brief 안건 아님.)
  - 안건 4 → **운영자 대안(정보 노출 파라미터 + 해결책 교체)**: 상대 예고에 「이기는 색」(우세 속성 1색) 상시 병기 · 삼각 한 줄(해금마다 5수) 폐지 · `winColorHintExchanges` 파라미터(프로토 ∞, 페이드 시점은 D3/M2) · 문제점 1 해결책 "10수 넘게 봤다" → "예고가 이기는 색을 알려주고 유저는 그 색의 초식을 손으로 친다(색 매칭)". 우세 *후보 아이콘* 강조 금지는 유지(속성 안내 ≠ 후보 강조). 연쇄 정정: ⑤ "제자가 δ 를 상쇄·열세로 버팀" → "우세로 깎되 파해(완파)는 4식 부재로 불가". (작성자 1안 「열세 체험 학습 + 도전자 속성 규칙」은 kill (b) 초반 표본 훼손으로 기각.)
  - 종합: 채택 3 / 폐기 1 → 본 04 재작성
- 자가 검증 로그 (cycle 04): (1) 성 산술 — 최소 경로 rankPts 7+14+24 = 45 = 12성 필요치(27+6+12), 표 145~205s 와 정합; 수련 포함 명시로 실전만 27pt 갈래 차단 ✓. (2) "이기는 색" 이 목적 (a)(d)·② 정보 노출·플레이 0~60s/1~3분·문제점 1·3 에 동일, 삼각 줄 참조 0건 ✓. (3) kill (b) 식이 목적 판정 형태·⑥ 에 동일(`oneTap=false`) ✓. (4) ⑤ δ vs 제자 = 우세·완파 불가 가 ④ 자동 선택 룰과 정합 ✓. (5) "우세 후보 아이콘 강조 없음" 은 유지하되 속성 안내와 구분해 서술 ✓.
- 사이클 이력: cycle 01 — blocker 0 / important 7, 보강 3 재작성 · debate 후보 4 작성자 해소 / cycle 02 — blocker 0 / important 6 (a3·b1·c2), 안건별 검토 6/6(채택 5 · 운영자 대안 1) / cycle 03 — blocker 0 / important 4 (a2·c2), 안건별 검토 4/4(채택 3 · 폐기 1) → 04 재작성. / cycle 04 — blocker 0 / important 4 (a2·c2), 안건별 검토 4/4(채택 4) → 05 재작성. minor/nit 폐기: 문제점 과추정 1건 · 파견 반짝임(`select.byUser` 사후 판독 경로 기존재) 등 — 내용 미출력(페르소나 사전 필터) minor/nit 폐기: 성립조건·kill 라벨 (a)~(d) 중복(nit, prefix 로 구분됨) · 문제점 4·6 과추정(minor, 추가 비용 0) · 문제점 8 실질 5단 노출(minor) · 해결책 4 "판단은 예고" vs 창 산출 "분기 판단 0.5s" 불일치(minor, D3) · 해결책 5 원 문제 재정의(minor, spec) · § ⑤ 유운보·4식 동일 쾌라 "분기 키 = 속성 선택" 1:1 불성립(nit, 4식은 사이클 밖)
- 사이클 cap 주의: cycle 04 = cap 3 초과 — 페르소나 재호출 후 important 만 남으면 종결 권유(debate 재분류 / spec 위임).
- 후속: (agenda_finalized 후 확정)
