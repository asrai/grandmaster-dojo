# 밸런스 튜닝 로그

이후 신설 회차는 판본 열을 반드시 채운다 — 판본 SoT 는 `src/balance.data.json` 의 `rev`. 과거 회차의 `–` 는 소급 추정 금지(#69)에 따른 미기재이며 조사 대상이 아니다.

**`first_transmit` 판독 주의 (#172 이후)** — 전수 실행이 화면 진입에서 S5 「전수하기」 버튼으로 옮겨졌다. 사람 표본의 이 값에는 그 버튼 앞에서 머문 시간이 새로 포함되고, 봇 표본은 그 버튼을 누르지 않고 코드로 직접 실행하므로 포함되지 않는다 — `#172` 전후 회차와 사람↔봇 회차를 이 열로 직접 비교하지 마라.

| 회차 | 일시 | 판본 (`rev`) | 변경 파라미터 | 이전 → 이후 | 관찰 (측정치) | 판단 |
|---|---|---|---|---|---|---|
| 0 | 08-31 | – | 초기값 | PRD § 3 표 | – | 기준선 |
| 0-a | 08-31 | – | (관찰) 헤드리스 하네스 1사이클 완주 | – | 수동 6/8/10 성공 → 전수 3종 → 일자진까지 가상시간 ~61초(입력 지연 0 가정, 실플레이 참고치 아님). 수련 시뮬 1시간 = +3,063 냥 — 진법 가격 300 냥 대비 10배, 사이클 후 재화 싱크 부재 | `simEfficiency` 0.3 → 0.05~0.1 하향 또는 사이클 2 싱크(도전자 등급·제자 슬롯) 추가를 9/3 튜닝 안건으로 |
| 0-b | 08-31 | – | (관찰) Chrome 실브라우저, 사람 속도 봇(반응 0.45~0.65s · 키당 0.26~0.38s · 15% 미입력) | – | **1사이클 2:11** (첫 전수 0:12 · 반격장 전수 1:00 · 천붕각 전수+일자진 2:11, 격파 19, 수동 24 · 커버 6 · 실패 12). 목표(첫 전수 ≤1:30 · 사이클 ≤5:00) 대비 **전부 빠름**. 실패 12 중 천붕각 자동 실패 3건은 남은 창(3000×0.55=1650ms)에 4키 커버가 빠듯해 미커버 | ① 유운보 임계 6은 "12초 만에 전수" — 훅으로는 좋으나 힌트 소멸 구간(3→6)이 3회뿐이라 "손이 기억한다" 경험이 성립 안 함 → 임계 8/10/12 또는 힌트 소멸을 임계 40% 지점으로 ② 긴 시퀀스일수록 `autoDelayRatio` 낮추기(0.45 → 0.35) 또는 초기 `autoEff` 0.7 ③ 사이클 2:11 은 여유 — 사이클 2(도전자 등급·제자 슬롯)로 5분을 채울지 9/2 결정 |
| 0-c | 09-01 | – | (관찰) 헤드리스 봇 v2 1사이클 × 40시드 + 브라우저 배선 1회 (DOM 스텁, 실브라우저 아님) | – | 40시드 중앙값 **사이클 241.7s** (199.0~293.7 · 300s 초과 0/40) · `first_fire_t` 3.2s (1.9~14.4) · `first_transmit_t` 216.2s (171.6~265.8, 예산 225s) · 완주율(b) 57.9% (45.7~72.7 · 50% 미달 7/40) · `ignore_rate` 6.2% (2.8~11.3 · 15% 초과 0/40) · 유효 성공률 57.9%. 브라우저 배선(DOM 스텁) 4회: 사이클 176.1 / 231.7 / 273.4 / **347.6**s · (b) 63.0 / 52.4 / — / 44.3% · `ignore_rate` 3.9~8.3% · 17종 전량 emit · 위반 0 | ① 선행 게이트 통과 — 입력 튜닝(버튼 히트·배치) 불요 ② 예산 가정 70% 대비 실측 58% 인데도 사이클이 300s 안에 드는 것은 봇이 창을 흘려도 재도전 손실이 없기 때문 — 09-02 사람 표본으로 재판독 ③ 완주율 하한 45.7% 는 `missRate` 0.15 가 5키(파운현월)에 0.85^5=44% 로 걸리는 지점이라, D3 에서 창 `T` 가 아니라 **`hintDelay`·시퀀스 길이 분포**를 먼저 본다 (창 감축은 `timeout`·`tail_ms` 게이트 뒤) ④ 브라우저 배선 4회 중 1회가 347.6s 로 300s 를 넘었다 — 완주율이 44%로 떨어진 회차라 예산이 완주율에 선형으로 걸려 있음을 보여 준다. 300s 는 **여유 있는 상한이 아니라 완주율 50% 언저리의 경계**이니, 09-02 사람 표본에서 (b) 가 50% 미만이면 (d) 도 함께 흔들릴 것으로 보고 두 축을 같이 판독한다 |
| 0-d | 09-01 | – | (관찰) **M1 정식 사람 표본** — 실크롬 셀프 플레이 1사이클, `#15` 유도 툴팁 랜딩(`689991fa`) 이후 판본 | – | **도장 체류 30.2s (전체의 15.7%)** — `0-c` 기준선 144.2s/44.9% 대비 **−114.0s (−79.1%)**. 두 덩어리 각각 최초 진입 53.2s → **3.8s**, 3차 대련 직후 35.7s → **2.1s**. `cycle_done_t` **189.3s** (기준선 321.0s · 임계 300.0s) · 완주율(b) **100.0%** (fire 32/32 · `timeout` 0건) · `ignore_rate` 4.6% (임계 15%) · `first_fire_t` 8.0s · `first_transmit` 150.4s · 창 잔여 64.5% (tail 1676~2320ms) · `oneTap` 0% · `select.byUser` 0% · 등급 {격파 34 · 상쇄 5 · 역파 3 · 우세/열세/피격 0}. 구간 분해: 대련 79.1s(41.1%) · 파견 30.8s(16.0%) · 도장 30.2s · 수련 27.1s · 결과 15.9s · 전수 4.9s. 채취 조건 전건 성립 (`tester_role=self` · `accessibility:false` · `accessibility_toggles:0` · `device` keyboard 단일 · `session` 1건 · 봇 미개입(채취자 증언) · `log_violations` 0 · 필드 결손 0 / 322건) | ① `#15` 성패 지표 **통과** — 도장 체류가 목표 방향으로 79% 줄었고 눈 확인 ⓐ(최초 진입 유도)·ⓑ(대련·전수·파견 3종 신규 활성화 고지) 전건 표시 확인 ② **감소분 전량을 툴팁 효과로 귀속하지 말 것** — 동일 채취자가 90분 전(`0-c` 기준선 회차)에 같은 빌드를 플레이했으므로 학습 효과가 교란한다. n=1 이고 분리 설계가 없다. 툴팁의 *기전*은 눈 확인으로 실증됐으나 *효과 크기*는 미확정이며, M2 에서 신규 피험자 표본으로 재측정한다 ③ (b)/(d) 결합 경고(`0-c` ④) **해소** — 완주율 100%·189.3s 로 「완주율 50% 언저리 = 300s 경계」에서 멀다. 봇 예산 가정(58%)과 사람 실측(100%)의 42%p 괴리는 봇의 `missRate` 0.15 가 사람 손을 과소평가한다는 뜻이므로, D3 튜닝에서 봇 수치를 그대로 난이도 근거로 쓰지 않는다 ④ **다음 시간 절감 여지는 도장이 아니라 파견 관전 30.8s** 다 — 사이클 내 비대련 단일 최대 덩어리가 되었고 그 구간의 유저 입력은 0건(`select.byUser` 0%)이다. 300s 예산에 여유가 111s 생겼으므로 즉시 착수 대상은 아니나, 사이클 2(도전자 등급·제자 슬롯) 설계 시 이 구간을 재화·선택 지점으로 쓸지 먼저 본다 ⑤ 툴팁 표현 방식은 별건 분리 — 현재 앵커가 통상 흐름에 세로 공간을 차지해 메뉴 레이아웃을 밀어낸다 (`#20`) |
| 0-e | 09-01 | – | `trainGraduateHits` · `threshold` · `rankStep` · 도전자 A HP (#38 성 적립 게이트 개정) | 3 → **2** / 4·4·5·5 → **2·2·2·2** / 3 → **2** (12성 45pt → **30pt**) / 40·55·70 → **30·45·80** | **사이클 시뮬 12시드 × 2 시나리오** (하네스 § 14-a, 봇 v2 손 모델). 유효 성공률 **72%**: 입문 186s · 12성 264s · `cycle_done` **285s** (191~371) / **47%**: 입문 248s · 12성 368s · `cycle_done` **395s** (269~628). 이정표 중앙값(72%) — 1식 100% 16s · 2식 104s · 3식 156s · 4식/입문 186s · 전수 266s. 밸런스 게이트: A-1 3초(유저 HP 100) · A-2 4초(100) · A-3 6초(96) 전승 · B 6초(제자 92). 기본 시드(`missRate` 0.15) 봇 v2 5회: 281/390/422/351/473s · 완주율 57/51/52/44/63% · `ignore_rate` 4.8~8.0% | ① **D3 시드표를 그대로 쓰면 kill (d) 를 깬다** — 시드(수련 3 · `threshold` 3·3·4·4 · `rankStep` 2 · A HP 45/60/110)는 72% 시나리오에서 481s 였다. 위 4종은 그 자리를 대체한 **시뮬 산출값**이다 ② A HP 는 시드와 반대로 **낮췄다** — 높이면 12초 상한 판정패가 늘고 그 재도전 비용이 「A3 를 Phase 2 무대로 길게 둔다」는 이득을 넘는다 (110 → 285s vs 80 → 248s, 47% 시나리오) ③ **47% 시나리오는 여전히 95s 초과** — 입문이 필수 경로가 되며 4식(5키·완주율 0.85^5)의 미완주 비용이 Phase 1 에 직접 실린 결과다. 남은 축은 4식 `threshold` 추가 하향 · `rankStep` 1(15pt) · 예고/연출 상수 절감 ④ 이 수치는 **봇 상계**다 — 봇은 원터치를 쓰지 않는데(`ai-log` 09-01 05:1x) 개정 규칙은 유저를 「3~4식이 100% 인 채로 도는」 구간에 오래 두므로 봇↔사람 괴리가 개정 전보다 커졌다. `0-d` 사람 표본이 완주율 100%·189.3s 였던 것과 같은 축이라, 09-02 사람 표본을 받기 전에는 이 초과분을 난이도 근거로 확정하지 않는다 |
| 0-f | 09-02 | `2026-09-02-a/fc2d116a` | 성 축 초식 단위 재설계 (#64) — 폐기 `masteryTrainPct`·`masteryFullPct`·`threshold`·`rankPtsPerStyle`·`rankStep`·`rankStepMult`·`equipMasteryPct`·`challengerPower` / 신설 `rankLadder`·`rankGate`·`challengerRank` | 숙련 0~100% + 무공 성 2축 → **초식 단위 성 1~12 단일 게이지** · 적립 3단(1~7 수련3/대련1 · 8~10 대련2 · 11 결정타 · 12 완파) · 계단 2/5/7 · 도전자 성 A-1 1 / A-2 2 / A-3 3 / B 2 (`challengerPower` A 1.0 → `powerOf(1)` 1.05 · A-2 1.1 · A-3 1.15 · B 1.1 → `powerOf(2)` **1.1 동일**) | **사이클 시뮬 12시드 × 2 시나리오** (하네스 § 14-a, 봇 v2 손 모델 — `0-e` 와 같은 스크립트를 성 축 이정표로 확장). 실현 63%: 해금 27s · 원터치 52s · 12성 98s · `cycle_done` **1019s** (533~1404) · 화면 최대 161 / 실현 31%: 해금 48s · 원터치 93s · 12성 201s · `cycle_done` **3367s** (921~6208) · 화면 최대 500. 밸런스 게이트 전승 유지 — A-1 3초(유저 HP 100) · A-2 4초(100) · A-3 6초(100) · B 6초(제자 92). 기본 시드 봇 v2 5회: 1081/930/2388/883/1686s · 완주율 50/51/41/55/42% · `ignore_rate` 5.4~7.1% | **⚠ 이 행의 관측치는 `0-g`(#65)에서 정정됐다** — 최초 기재는 최종 코드 변경 전 회차의 수치였다 (50% 시나리오 실현 41%→**31%** · 12성 173→**201s** · `cycle_done` 1187→**3367s** · 하한 840→**921s**, 70% 상한 1295→**1404s**, 봇 v2 5회 전건). 시뮬은 `createSeededRandom` 고정 시드라 재실행이 축자 동일하므로 비결정성이 아니라 갱신 누락이며, 위 값은 랜딩 tip `2a068d2` 재실행 실측으로 갈아 끼운 것이다. ① **kill (d) 300s 를 시뮬 목적함수의 제약으로 넣지 않았다** (운영자 판정 기준 지시 ① · 이슈 #64 § 결정 사항) — 위 사이클 시간은 관측치이지 통과 여부가 아니다. `0-e` 가 손 추정 시드표를 그대로 밀어 481s 를 낸 전례의 반대 방향 조치라, 이 회차는 **시드를 시간에 맞춰 깎지 않았다** ② 사이클이 `0-e`(285s/395s) 대비 3~11배로 늘어난 것은 규칙 자체의 성질이다 — 11·12성이 적립이 아니라 **초식마다 결정타 1회 + 완파 1회**를 요구하고, 전수 조건이 전 초식 12성이라 그 사건을 4번씩 기다린다. 삭감 여지는 콘텐츠가 아니라 후속 유닛(#65 재대련·A-4)이 만들 무대 밀도에 있다 ③ **완주율(b)이 51%→39~50% 로 내려간 것은 입력부 회귀가 아니라 봇 전략의 산물이다** — 봇에 「계단을 미는 손」(10성 초식이 있으면 그것을 낸다)을 넣지 않으면 결정타가 그 초식에 영영 안 떨어져 사이클이 멈춘다(실측: 시드 7919 가 4000화면에서 미완주). 그 손이 5키 파운현월을 자주 고르고 `missRate` 0.15 가 5키에 0.85^5=44% 로 걸린다. 사람은 같은 자리에서 원터치를 쓰지만 봇은 kill (b) 표본 보호 때문에 쓰지 않는다(`ai-log` 09-01 05:1x) — 봇↔사람 괴리가 `0-e` ④ 보다 더 커졌으니 이 수치를 난이도 근거로 쓰지 말 것 ④ 도전자 성으로 통합하며 **B 의 내공은 1.1 로 정확히 보존**됐고(REQ-506 무패 보장 게이트 무영향) A 군만 1.0 → 1.05/1.1/1.15 로 올랐다 — A 밸런스 게이트는 4구성 전부 전승 유지라 HP 재조정 없이 통과 |
| 0-g | 09-02 | `2026-09-02-b/50480cbd` | 사부 대련 확장 (#65) — 신설 `hp['A-4']` 90 · `challengerRank['A-4']` 4 · `rematch{rankGain:1, rankCap:3}` · `reversalDecay{perRank, pierceFloor}` / 봇 무대 선택 `nextDuelStage` · `preferLadderPush` 조건 개정 | A 도전자 3차 → **4차 (α·γ·δ, 절초 δ 의 사부 첫 무대)** · 재대련 성 +1 누적(상한 +3, 재화 0) · 역파 감쇠 신설 (`decayPerRank` 시드 0.05 → **0.075**, 관통 하한 0.4 고정) | **사이클 시뮬 12시드 × 2 시나리오** (하네스 § 14-a, `0-f` 와 같은 스크립트). 실현 69%: 해금 25s · 원터치 59s · 첫 12성 284s · `cycle_done` **508s** (368~614) · 화면 최대 110 · 재대련 진입 중앙값 12회(최심 13번째 대면) / 실현 41%: 해금 46s · 원터치 100s · 첫 12성 515s · `cycle_done` **862s** (715~1294) · 화면 최대 154 · 재대련 진입 16회(최심 8번째 대면). **시드 0~499 완주 스윕 500/500** (최대 170화면). A 밸런스 게이트 전승 — A-1 3초(유저 HP 100) · A-2 4초(100) · A-3 6초(100) · **A-4 8초(90)** · **상한 강화 A-4(도전자 성 7) 6초(90)** · B 6초(제자 92). 기본 시드 봇 v2 5회: 547/506/774/645/711s · 완주율 56/59/48/53/49% · `ignore_rate` 4.2~6.7% | ① **선행 회차 `0-f` 를 먼저 정정했다** — 기재값이 랜딩 tip `2a068d2` 재실행 실측과 어긋나 있었다(50% 시나리오 `cycle_done` 1187s→**3367s** 등, 위 행 참조). 고정 시드라 재현이 축자 동일하므로 갱신 누락이었고, 이 회차의 비교 기준이 그 숫자라 교정이 선행 조건이었다 ② **kill (d) 300s 는 이번에도 목적함수의 제약이 아니다** (운영자 판정 기준 지시 ①) — 아래 사이클 단축은 시간을 겨냥한 튜닝이 아니라 봇 데드락 수정의 부수 효과다 ③ **A-4 를 더했는데 사이클이 3367s→862s 로 줄었다** — A-4 도입이 봇의 고착 두 종을 드러냈기 때문이다. ⓐ 무대 고착: 적운압정의 파해 대상 β 가 A-4 예고에 없는데 봇이 최고 차수만 반복해 10성에서 영구 정지(6000화면 미완주 실측) → 무대 선택 `nextDuelStage` + 계단 푸시 조건 한정. ⓑ 손 굶주림: 「이기는 색」 동률을 슬롯 순으로 깨는 바람에 같은 쾌인 파운현월이 유운보의 창을 독점해 유운보가 8성에 고착(시드 0~499 중 6건 미완주, L5d 적대 리뷰 지적) → 동률을 **덜 여문 초식** 우선으로 뒤집었다. 두 고착을 걷어내자 `0-f` 가 잰 것이 규칙의 비용이 아니라 **봇 전략의 비용**이었음이 드러났다 — `0-f` ② 의 「규칙 자체의 성질」 해석은 그만큼 과대평가였다. 첫 12성 시점이 95s→284s 로 늦어진 것은 그 반대 방향이 아니라 같은 축이다: 한 초식을 몰아 키우는 대신 넷을 고르게 키운다 ④ **`decayPerRank` 시드 0.05 는 관통 하한을 사표로 만든다** — 하한 0.4 가 물리려면 성 차 12 가 필요한데 역파 무대의 정의역 상계는 8 이다(사부 12성 vs A-4 성 4 · 제자 10성 vs B 성 2). 0.075 로 올려 하한이 정확히 그 상계에서 물리게 했다(`ai-log` 09-02 05:4x) ⑤ **A-4 HP 90 · 도전자 성 4 는 시드 그대로 유지**했다 — 성 1 유저가 8초(HP 90 잔존)에, 재대련 상한 강화(성 7)에서도 원터치 성 유저가 6초에 잡아 게이트가 양쪽에서 통과했다. 난이도는 HP·도전자 성으로만 조정한다는 REQ-771 의 분업도 그대로다 ⑥ 재대련 관측치(중앙값 15~20회)는 **봇 산물이지 사람 예측이 아니다** — 봇은 계단 무대를 얻으려 낮은 차수로 되돌아가고, 그 승리는 재화가 0 이라 파밍이 되지 않는다. 사람의 중단 지점은 `rematch.attempt_n` 분포로 판정 세션에서 잰다 |
| 0-h | 09-02 | `2026-09-02-c/309c143a` | 제자 축 확장 (#66) — 신설 `discipleTrain{secondsPerRank:1800}` · `mission{unlockRank:5, foeCount:3, rankStep:1}` · `killReadout{minManualWindows:20}` / 방치 압축 버튼(`simTrainSeconds`)이 제자 수련 시계로 연결 | 파견 1단(고정 B) → **2단 (B-1 고정·무패 보장 / B-2+ 랜덤 임무 + 최소 성 하드 잠금)** · 제자 성장 = 파견 적립 전용 → **제자 수련(병렬 방치, 1~7성) 추가** · 임무 도전자 성 B-1 2 → B-N 2+(N−1) | **사부 축 무회귀 (0-g 와 축자로 동일)**: 봇 v2 5시드 사이클 547/506/774/645/711s · 완주율 56/59/48/53/49% · `ignore` 4.2~6.7% · 70% 시나리오 해금 25s·원터치 59s·12성 284s·`cycle_done` 508s [368~614] / 50% 시나리오 862s [715~1294] · A-1~A-4·B 게이트 전건 통과 (B 시뮬 6초, 제자 HP 92 잔존). **신규 축 (판정 범위 밖 셀프 관측, REQ-792)**: 5시드 전건 B-1 승 → B-2·B-3·B-4 전건 승 (도전자 성 3/4/5, 조합은 시드마다 다름 — δ 포함 회차 다수). B-2 잠금 해제까지 **제자 수련 11성 = 실경과 5.5시간** (5시드 전건 동일 — B-1 파견 적립이 16성 중 5성을 먼저 채운다). `rank_wall` 0건 (봇은 5성에서 멈추므로 7성 벽에 닿지 않는다). 판독기 `metrics.disciple_train_activity` = `{dojo:11}` — 헤드리스 구동은 도장에서만 시계를 앞당기므로 병렬성의 **혼합 활동 표본은 여기서 나오지 않는다** (하네스가 `duel` 귀속을 단정으로 별도 검증) | ① **`discipleTrain.secondsPerRank` 1800 을 시드 그대로 확정**했다 — 판정 세션의 대기 지루함은 판정 범위를 B-1 로 긋는 것(REQ-792)·치트 패널·하네스 시간 주입으로 해소하고 게임 수치를 깎지 않는다. 5.5시간이라는 관측치는 「방치 루프가 재방문 동기로 성립하는 길이인가」의 입력이지 축소의 근거가 아니다 (kill (d) 300s 를 목적함수 제약으로 넣지 않는 것과 같은 축 — 운영자 판정 기준 지시 ①) ② **`mission.rankStep` 1 은 느슨하다** — B-4(도전자 성 5)까지 5시드 전건 승이라 눌러앉기는 막았어도 「지금 제자 성으로 보낼까」의 긴장이 아직 없다. 다만 이 축은 판정 범위 밖이고 곡선을 세우려면 임무 종류·보스(M2)가 함께 와야 하므로, M1 에서는 **골격만 두고 값을 올리지 않는다**. 올릴 자리는 `rankStep` 하나이며 JSON 만 고치면 된다 ③ **`mission.unlockRank` 5 는 B-1 적립과 맞물려 「수련 11성」으로 떨어진다** — 잠금 기준을 6 으로 올리면 방치가 곧바로 2배가 되므로, 잠금은 시간이 아니라 **뒤처진 초식을 지목하는 장치**로 두고 5 를 유지한다 ④ **`killReadout.minManualWindows` 20 은 아직 한 번도 물리지 않았다** — 봇 사이클의 수동 창이 105건이라 여유가 크다. 이 가드는 봇이 아니라 **원터치 성이 오른 사람 표본**을 겨눈 것이라(모집단이 7성 이후 급격히 줄어든다) 09-02 판정 세션에서 처음 시험된다 |
| 0-i | 09-02 | `2026-09-02-c/309c143a` | (렌더 층) 밸런스 tunable 무변경 — 신설분은 전부 `index.html` `:root` 시각 토큰이다: `--only-hold` · `--slip-exit` · 3단 고정 50/440/362 · 아레나 좌표 · 죽간 폭 계단 · 키패드 규격 (#96 S1 대련 이식) | 판정 재생 앞 예산 히트스톱 80ms → 히트스톱 80 + 확정 연출 대기 120 = **200ms** (재생 `resolveMs` 500ms 안) | **헤드리스 봇 페이스 `0-h` 와 축자 동일** — 봇 v2 5시드 사이클 547/506/774/645/711s · 완주율 56/59/48/53/49% · `ignore` `4.2~6.7%` · 70% 시나리오(실현 69%) 해금 25s·원터치 59s·12성 284s·`cycle_done` 508s `[368~614s]` / 50% 시나리오(실현 41%) `cycle_done` 862s `[715~1294s]`. 대조 방법 = 헤드리스 페이스를 결정하는 층(`bot.mjs`·`core.mjs`·`balance.mjs`·`balance.data.json`·`log.mjs`·`match`·`session`·`wiring`·`sequence-input`·`theme.mjs`)의 `git diff origin/main` 이 0바이트임을 확인한 뒤 하네스 재실행 — 단정 4814건·실패 0건. **사람 속도 실브라우저 봇 표본은 미채취** | ① 이 사이클은 렌더 층이라 헤드리스 페이스 회귀가 **원리적으로 0** 이고, 위 축자 동일은 그 확인이지 튜닝 결과가 아니다 — 재설계 전후 비교의 「전」이 곧 `0-h` 행이다 ② `--only-hold` 120ms 는 시안 실측 전 초기값이다. 판정 앞 예산이 200/500ms 를 먹으므로 남는 재생이 300ms 이고, 그 합이 재생을 남기는지는 `app.mjs` 부팅 단정이 문다 — 값을 올릴 때 그 단정이 먼저 터지는 것이 설계다 ③ **사람 표본은 이 회차에 없다** — 무인 레인에 실브라우저가 없어 `docs/balance-log.md` 의 사람 속도 봇 1회는 L4 로 이월했다. 죽간 exit 전이(REQ-825)가 오히려 좁혀짐을 흐리는지, `.only` 120ms 가 판정을 늦게 느끼게 하는지는 그 표본에서만 갈린다 — 흐리면 `--slip-exit` 부터 줄이고, 그래도 방해되면 그 사실을 이슈에 남긴다(조용히 빼지 않는다) ④ 3단 50/440/362 · 죽간 폭 84/104/132/172 · 키 64×58 은 전부 **시안 실측 초기값**이라 튜닝 대상이다 |
| 0-j | 09-02 | `2026-09-02-c/309c143a` | (렌더 층 + DOM-free 반환 형태) 밸런스 tunable 무변경 — 신설분은 `index.html` `:root` 시각 토큰(`--beckon-dur` · `--visit-w` · 수련장/족자/지켜보는 사부 좌표)뿐이고, DOM-free 층은 `selectDiscipleStyle` 이 `{style, reason}` 을 반환하도록 형태만 넓혔다 (#97 S3 수련·S4 파견 이식) | 선택 규칙 무변경 — 반환하던 `style` 이 `.style` 로 한 겹 들어가고 그 옆에 `reason` 3계열이 붙었을 뿐 | **헤드리스 봇 페이스 `0-i` 와 축자 동일** — 봇 v2 5시드 사이클 547/506/774/645/711s · 완주율 56/59/48/53/49% · `ignore` `4.2~6.7%` · 70% 시나리오(실현 69%) 해금 25s·원터치 59s·12성 284s·`cycle_done` 508s `[368~614s]` / 50% 시나리오(실현 41%) `cycle_done` 862s `[715~1294s]` · A-1~A-4·B 게이트 전건 통과(B 6초, 제자 HP 92). 하네스 단정 4825건·실패 0건 (`0-i` 4814건 + 이유 3계열·구결 1:1·속성 커버리지 신규 11건). 대조 방법 = `0-i` 처럼 diff 0바이트 확인이 **불가능한 회차**라(`bot.mjs`·`core.mjs`·`balance.mjs` 가 실제로 바뀌었다) 고정 시드 재실행 수치를 `0-i` 행과 직접 대조했다. **사람 속도 실브라우저 봇 표본은 미채취** | ① 이 회차의 페이스 축자 동일은 「렌더 층이라 원리적으로 0」이 아니라 **선택 규칙을 건드리지 않았다는 실측 증거**다 — `0-i` 와 성격이 다르므로 같은 근거로 읽지 말 것. `selectDiscipleStyle` 은 고르는 초식이 아니라 그 이유를 함께 낼 뿐이고, 봇 사이클은 이유를 읽지 않는다(`chooseStyle` 이 `.style` 만 꺼낸다) ② **`--beckon-dur` 1100ms 는 시안 실측 전 초기값**이고 판정 앞 예산(`--juice-hitstop` + `--only-hold` = 200/500ms)과 **무관한 축**이다 — 죽간 유도는 판정 재생과 겹치지 않는 예고 구간의 연출이라 `app.mjs` 부팅 단정에 닿지 않는다 ③ 수련장/족자/지켜보는 사부 좌표(`--fig-hall-*` · `--scroll-*` · `--watch-*`)도 전부 시안 실측 초기값이라 튜닝 대상이다 — 특히 `--watch-y: -144px` 은 「앞에서 잘린 뒷모습」의 잘리는 양을 정하는 값이라 실브라우저에서 처음 판단된다 ④ **사람 표본은 이 회차에도 없다** — 무인 레인에 실브라우저가 없어 파해 유도 금색 맥동이 「유도」로 읽히는지(REQ-855), 수련 실패 낭독이 스크린 리더에 실제로 닿는지(#51), 관전 중 「제자의 판단」이 재촉으로 읽히지 않는지(REQ-407)는 `task/l4-field-test` 로 이월했다 |
| 0-k | 09-03 | `2026-09-03-a/d0243637` | (관찰) 밸런스 tunable 무변경 — L4 실브라우저 순회(#106·112·116·118·123·125)에서 채취한 **사람 속도 봇 실브라우저 1사이클**. `0-i`·`0-j` 가 비워 둔 자리다 | – | **Chrome 실브라우저 봇 v2 1사이클** (macOS · 창 500×1029 → `#stage` 1:1 393×852 · `?tools=0` · Claude in Chrome 확장 부착 · 빌드 `1c9a152`): `cycle_done_t` **708.7s** · 완주율(b) **54.7%** = fire(oneTap=false) 82 / (+`timeout` 68) · 수동 창 150 · `ignore_rate` **6.3%** (device keyboard 6.3%) · `first_fire_t` **8.2s** · `first_transmit` **690.2s** · 창 잔여 45.6% (tail 1184~1640ms) · `oneTap` **0.0%** · `select.byUser` 0.0% · top_attr 전환 159회 · 등급 {완파 57 · 우세 29 · 상쇄 1 · 열세 0 · 역파 0 · 피격 68} · 재대련 회차 {2:4, 3:4, 4:2, 5:1, 6:1, 7:2, 8:1} 최심 8 · 8성 벽 0회 · 파견 B-1 win\[alpha+gamma+delta\] 승률 100% · 판독 **green** (필드 결손 0 / 1497건 · 통합 로그 스키마 20종 전량 emit). **프레임 예산 3세션** (같은 기기, 봇 구동): `scene:"verdict"` p95 중앙값 18.3 / 17.9 / 18.2ms → **3세션 중앙값 18.2ms** (dropped 1 / 0 / 0) · `scene:"parallax"` 18.2 / 18.0 / 18.1 → **18.1ms** · `scene:"idle"` 18.2ms. **실기 아님 · CPU 스로틀 미적용** | ① **`0-i`·`0-j` 의 빈자리가 채워졌다 — 다만 표본은 봇이지 사람 손이 아니다.** `oneTap` **0.0%** 가 그 경계다(봇은 원터치를 쓰지 않는다, `0-f` ③). `0-d` 의 사람 셀프 플레이(완주율 100% · 189.3s)와 이 회차(54.7% · 708.7s)의 차이를 난이도 회귀로 읽지 말 것 — 손 모델이 다르고 `0-d` 이후 성 축 재설계(`0-f`)가 끼어 있다 ② **렌더 층이 붙어도 페이스가 무너지지 않는다** — 708.7s 는 `0-h`~`0-j` 의 헤드리스 5시드(547/506/774/645/711s, 중앙값 645s) 대역 안이다. 재설계 3회차(`0-i`·`0-j`)가 「원리적으로 0」·「선택 규칙 무변경」으로만 논증했던 것이 실브라우저 실측으로 처음 확인됐다 ③ **판독기가 kill (d) 를 「판독 불가」로 표시한다** — 페이지 로드 직후 ~2s 가 사람 구간으로 잡히고 그 뒤 봇이 이어받아 「손 인계」로 분류됐다. 708.7s 는 kill (d) 판정이 아니라 관측치다 ④ **프레임 예산은 이 회차로 확정하지 않는다** — `verdict`·`parallax`·`idle` p95 가 17.9~18.3ms 로 전부 붙어 있어 juice(흔들림 420ms + 96px 글로우 + 스크림)의 비용이 측정에 잡히지 않고, #100 헤드리스 16.7ms 대비 균일한 +1.5ms 는 장면 부하가 아니라 확장 부착 창 오버헤드로 읽힌다. **`parallaxMinFps` 50 은 잠정값 그대로 둔다** — 임계 근처를 내는 기기가 표본에 없다 (L4 #125) ⑤ 같은 순회에서 렌더 결함 3건이 나왔다 — #137(전수 실루엣 미렌더) · #138(죽간 탈락 전이 좌표 튐) · #139(도장 12성 행 초식명 줄바꿈). 셋 다 **밸런스 tunable 무관**이라 이 행의 수치에 영향이 없다 |
| 0-l | 09-04 | `2026-09-04-a/cb8d7b38` | 파견 상대 추출 (#217) — `reversalDecay.perRank` 0.075 → **0.1** / 폐기 `mission.foeCount` | 파견 상대 = 고정 B(HP 80 · α γ δ) → **1차 A-4 고정(HP 90) · 2차부터 사부가 이긴 도전자 랜덤** · 임무 도전자 성 base 가 B(2) → 뽑힌 도전자의 성 | **헤드리스 1사이클 5시드 (같은 스크립트로 `origin/main` 8efaed6 대조)**: 552.9 / 603.9 / 762.3 / 562.9 / 877.8s ← 546.1 / 597.2 / 755.5 / 556.2 / 871.0s — **전건 +6.8s** (1차 파견 상대가 B 6초·HP 80 에서 A-4 7초·HP 90 으로 바뀐 몫). 5시드 전건 B-1 상대 A-4 · B-2~B-4 상대 시드마다 갈림(A-1~A-4) · 파견 20판 전승. **로스터 4명 전원 1성 제자 무지시 파견 시뮬** (하네스): A-1 2수(제자 HP 100) · A-2 3수(100) · A-3 5수(100) · A-4 7수(90) — 전건 승 · 역파 0건. 하네스 단정 5257건·실패 0건 | ① **`perRank` 상향은 난이도 튜닝이 아니라 검증기 교정의 종속 변경**이다 — `checkReversalReach` 의 무대 집합을 바로잡으면 A-4 제자 무대(최대 성 차 6)가 하한 결속점 8 에 못 닿아 부팅이 터진다. 0.1 은 검증기 자신이 낸 처방값이고 여유 0(0.6/6)이라, 앞으로 `discipleRankMax` 를 내리거나 A-4 성을 올리면 즉시 다시 물린다 — 그 즉시성이 이 값의 존재 이유다 ② **역파 피격은 두 무대 모두 줄어드는 방향**이다 (A-4 상대 역파 피격: 성 차 5 에서 15 → 12, 성 차 7 에서 11 → 10, 최대 성 차 8 에서는 하한 0.4 로 10 동일). 무패 보장을 위협하지 않지만 「절초는 무서워야 한다」(REQ-771)의 체감이 중간 성 구간에서 옅어지므로, L4 사람 표본에서 역파 한 방의 인상을 별도로 본다 ③ **`mission.rankStep` 1 은 이번에도 올리지 않았다** (`0-h` ② 승계) — 난이도의 몸통이 성이 아니라 **HP·초식 수**임이 이 회차로 실측됐다(성만 1→12 로 쓸어도 초 수 6 불변, 제자 HP 차 4점). 곡선을 세울 자리는 `rankStep` 이 아니라 M2 임무 콘텐츠다. 그래서 **차수와 체감 난이도가 단조가 아닌 것은 이 판본이 수용한 성질**이다 — 추출이 그 몸통(A-1 HP 30·2초 ↔ A-4 HP 90·7초)을 무작위화하므로 B-5 가 B-2 보다 쉬울 수 있고, 그 단조성은 M2 임무 콘텐츠가 진다 ④ **B-2 파견의 상대 분포는 이 표본으로 판정하지 않는다** — 추출은 균등이지만 헤드리스가 쓰는 LCG(`createSeededRandom`)는 작은 시드의 첫 출력이 서로 붙어 있어 5시드 전건 B-2 가 A-1 로 나왔다. 프로덕션은 `Math.random` 이라 해당 없고, 분포 관측이 필요하면 한 시드 안에서 여러 번 굴려야 한다(하네스 40회 추출 단정이 그 형태다) |
