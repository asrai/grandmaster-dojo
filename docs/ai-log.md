# AI 활용 내역 — 채택 / 수정 / 폐기 기록

규칙: 결정 시점에 즉시 기록 (사후 재구성 금지). 열 = 일시 · 단계 · AI 제안 · 판단 · 이유. 원문 대화는 Claude Code 세션 로그 export (`logs/`) 참조.

| 일시 (KST) | 단계 | 도구 | AI 제안 | 판단 | 이유 |
|---|---|---|---|---|---|
| 08-31 15:0x | 장르 선택 | Claude Code (Fable 5) | 방치형은 적합하나 순수 방치형 대신 "아이들 아케이드(조작 + 자동화 해금)" 권고. 미니게임 1종 트랙을 차선으로 제시 | **채택** | 평가자 3~5분 플레이 제약 + JD "손맛" 키워드. 순수 방치형은 재미 가설 시연이 약함 |
| 08-31 15:1x | 컨셉 후보 | Claude Code + Explore 서브에이전트 | 볼트 아이디어 29건 스크리닝 → 상 10건 → 3안(초식 도장 / 퍼즐용사 / 마다라) + 예비 배틀 레이싱 | **채택 (A안)** | 원본 "모바일 무협"에 수동→숙련도→자동 구조가 이미 있어 변환 손실 최소. B안은 9/2 피벗 fallback 으로 보존 |
| 08-31 15:2x | 원본 미정 항목 결정 | Claude Code | 초식 매칭 규칙: 가위바위보/상성표/시퀀스 길이 중 → **1:1 고정 상성 + 시퀀스 입력** | **채택** | 3~5분 안에 읽기 부담 없이 손맛만 남기려면 판단(상성 추론)보다 실행(시퀀스)에 집중 |
| 08-31 15:2x | 원본 미정 항목 결정 | Claude Code | 진법을 "제자 배치 버프 1종"으로 축소 | **수정 채택** | 원본은 진법 수집·운용이 핵심 재미라 했으나 1주 스코프·1사이클 시연에는 과함 → 1사이클 완료 마커로만 사용, 수집 축은 Scope-out 명시 |
| 08-31 15:2x | 밸런싱 초기값 | Claude Code | 임계 6/8/10, 창 2200/2600/3000ms 등 초기 표 | **잠정 채택** | 근거는 입력 1타 600ms 가정 — 9/3 튜닝 회차에서 실측으로 교체 예정 |
| 08-31 15:2x | 방치 축 시연 | Claude Code | "1시간 수련 시뮬" 버튼으로 오프라인 보상을 즉시 체감 | **채택** | 평가자가 실제로 방치할 수 없으므로 압축 장치 필요 |
| 08-31 16:0x | 검증 | Claude Code + Chrome 자동화 | 헤드리스 하네스(논리) + 실브라우저 사람 속도 봇(페이스) 2단 검증, 결과를 balance-log 0-a/0-b 로 기록 | **채택** | 테스터 모집 전 페이스 기준선 확보. 봇 수치는 사람 실측을 대체하지 않으므로 '참고치'로 표기 |
| 08-31 16:5x | 네이밍 | 사용자 결정 | 게임명 「초식 도장 (Stance Dojo)」 → **「용쟁호투 (龍爭虎鬪)」** 로 변경. 볼트 제안서 폴더·README·PRD·index.html 일괄 반영 | **채택** | 사용자 지시. 위 08-31 15:1x 행의 "초식 도장" 은 당시 후보명 기록으로 보존 |
| 08-31 17:3x~18:3x | 설계 v2 | 사용자 주도 + Claude Code (Fable 5) | 사용자: 사부·제자를 후속으로 미루고 "유저가 직접 고수가 되는 과정" + 후보 필터 시퀀스 입력(자동완성식) + 100% 원터치 + 학습/실전 힌트 분리. Claude: 제자 제거 시 "자동화 해금" 축이 사라지는 문제 지적 → 100% 자동응수 토글로 복구 제안, 발동 규칙에 **포함 금지 규칙**(한 초식 시퀀스가 다른 초식의 완전 접두어 금지) 필요성 제시, 오입력=무시(리셋 아님) 제안 | **채택 (통합)** | 사용자가 "일대종사 3막"(수련→전수→종사)으로 재구성해 제자를 2막으로 복귀시켜 두 요구를 통합. 포함 금지는 4방향 3길이만 64개라 공간 제약 없음을 수치로 확인 후 규칙 채택. 볼트 `일대종사_00_아이디어_v2.md` |
| 08-31 18:0x | 설계 v2 | 사용자 제안 | 실전 슬롯 제한(레벨별 3→8) — 배운 초식은 수십 개라도 장착은 N개, 새 비급을 배우려면 자리를 비워야 함 | **채택** | Claude 평가: 슬롯 제한이 "전수"의 동기가 됨(슬롯 차면 100% 초식을 제자에게 넘겨야 다음 학습). 첫 키 전 후보 접기 UI 규칙도 불필요해짐 |
| 08-31 18:2x | 공방 판정 | Claude Code | 5-3(4단 판정)을 6단으로 확장(열세·역파 추가, 속성 삼각 쾌>강>정>쾌), 선기 +30%, 빈틈 상태, 파라미터 10종 초기값 | **채택** | 4단에서는 "무관 초식 완주 = 안전"이라 후보 필터 화면이 안전 경로를 노출함. 볼트 `일대종사_공방 판정 설계.md` 가 SoT |
| 08-31 18:5x~19:0x | /gamedev-concept R2 | Claude Code 페르소나 CEO/CFO/CTO | `--force` 재평가: 8.0 / 8.0 / 7.75 pass. R1 피드백 12건 중 9건 LLM 자동 수정안 반영(HP회복 광고슬롯→원 환불, rAF 독립 정산, 심사 버퍼, 운영 손익분기 등), 타겟 25~40→18~34, 경쟁 훅 웹 조사(도장 아이들 3종, 숙련 게이트 훅 미발견) | **채택** | 볼트 `일대종사_02_제안.md` / `_02_평가.md`. R2 피드백 중 D1 반영: 판정부 데이터 드리븐 JSON + 하네스 단언 3종 |
| 08-31 18:5x | 엔진 ADR | CTO 페르소나 권고 Unity vs 사용자 결정 | CTO(R1): 모바일 IAA 미디에이션 생태계 근거로 Unity 권고 | **미채택 → HTML5 vanilla 유지** | 사용자 결정. 1인 개발 이식 비용 0, 상용 1차는 웹 포털/PWA, 2차 Capacitor 래핑. CTO(R2)는 웹뷰 입력 지연 실측 미흡수 또는 eCPM 열위 시 Unity 이식을 재결정 트리거로 조건부 재권고 — 제안서 § 9 ADR 에 미채택 사유 기록 |
| 08-31 19:1x | 네이밍 | 사용자 결정 | 게임명 「용쟁호투 (龍爭虎鬪)」 → **「일대종사 (一代宗師)」**, 영문 워킹 타이틀 **Grandmaster Dojo**. 볼트 폴더·노트 9개·README·PRD·index.html·repo 이름(`grandmaster-dojo`) 일괄 반영 | **채택** | 용쟁호투는 대련 한 장면을, 일대종사는 3막 구조의 도착점(플레이어 판타지·문파 개창)을 가리킴. R2 CEO#1 "일대종사가 Tier-1 에겐 단어일 뿐 → Grandmaster 로컬라이즈가 그림" 피드백과 정합. 영문 "Grandmaster" 단독은 체스 ASO 충돌 → Dojo 결합 |
| 08-31 19:0x | 엔진 확정 | Claude Code (Fable 5) | 사용자 질문 "HTML5 vs Unity WebGL 결과물 퀄리티 차이?" → 이 아트 방향(기호·실루엣·궤적)에선 차이가 엔진이 아니라 주스(히트스톱·흔들림·글로우)에서 나고, Unity WebGL 은 첫 로딩 5~15MB·모바일 브라우저 불안정·헤드리스 검증 불가로 과제 조건에 불리하다고 분석. 부족 시 PixiJS 중간 선택지 제시 | **채택 — HTML5 확정** | 사용자 결정. Unity 재검토 트리거에 "아트 방향 격상(스켈레탈 애니·셰이더)" 추가, D4 에 주스 체크리스트 6종 고정. 볼트 `일대종사_엔진 결정 (HTML5 vs Unity WebGL).md` |
| 08-31 19:2x~19:4x | 인프라 | /gamedev-init (Claude Code) | 기존 repo 를 입양하는 모드가 없어 A안 채택: 옛 repo 를 `grandmaster-dojo-archive` 로 개명해 이름 확보 → 같은 이름으로 재생성 → 옛 히스토리 4 커밋 승계(pull) → 볼트 슬롯 `13. 일대종사` (Layout v2 6 폴더) + 제안서·구상 이력 이관 + `2. 프로젝트 소개`(repo_name 앵커) + CLAUDE.md § Vault Reference + 표준 워크플로·하드닝·ruleset·OAuth·glossary 시드. 엔진 필드는 점수 규칙(Godot 6)과 달리 ADR 대로 HTML5 로 기록, variant 부재는 `docs/TODO-workflow-variants.md` | **채택** | 이후 `/gamedev-roadmap·brief·spec·build·check·qa` 가 슬롯 `repo_name` → repo `DesignRoot` 사슬로 위치를 해석하므로 파이프라인(=포트폴리오 기록) 전제. 삭제 대신 개명은 `delete_repo` 스코프 부재 우회 + 원격 히스토리 보존 |
| 09-01 02:2x | 프로토타입 v2 구현 ① | Claude Code (Opus 5, 무인 워커) | v0 의 `index.html` 단일 파일 유지 vs 순수 로직·데이터를 `src/*.mjs` (ESM) 로 분리. 분리안: `index.html` 이 `<script type="module">` 로 로드하고 헤드리스 하네스는 Node 에서 같은 모듈을 import | **채택 (분리)** | 단일 파일로는 하네스가 DOM 없이 순수 함수를 import 할 수 없어 REQ-205·505·506 과 수용 케이스 5·6·8 이 성립 불가. 빌드 단계 0·의존성 0 은 ESM 분리 후에도 유지되므로 엔진 ADR 의 실질(무빌드·무의존)은 보존. 대가는 `file://` 직접 열기 불가 — 변경 전 CLAUDE.md 는 직접 열기도 허용했으므로("Open it directly or serve with…") 무영향이 아니라 실제 축소이고, 그래서 같은 PR 에서 CLAUDE.md 를 로컬 서버 전용으로 정정했다. README 실행법 동기화는 유닛 3 소관 |
| 09-01 02:2x | 프로토타입 v2 구현 ① | Claude Code (Opus 5, 무인 워커) | spec 미지정분 3종을 구현 시점에 확정: ⓐ 도전자 절초 δ 의 파해 대상 = 유운보 (역파의 유일한 발생원) ⓑ 유운검법 2~4식 이름·한자·구결 + 도전자 초식/유파명 ⓒ 빈틈 중 완파 취급은 빈틈을 다시 열지 않음 | **채택** | ⓐ 정(δ)>쾌(유운보) 속성 삼각과 정합 — 「느리지만 읽어 받아친다」가 δ 가 파하는 대상이 쾌 계열이어야 함을 요구한다 ⓑ 조사 노트 §2-5 자연현상 명명 관례를 따름 ⓒ 재개방을 허용하면 연환이 무한히 이어져 전투가 성립하지 않음 (하네스가 이 단정을 pin) |
| 09-01 02:4x | 프로토타입 v2 구현 ① | Claude Code `code-reviewer` (L5) | 제자 자동 선택의 역파 회피 필터가 절초 예고 여부와 무관하게 상시 적용돼, 절초가 안 나온 수에도 α 의 파해 초식(유운보)이 후보에서 빠져 도전자 B 전투 3수가 0피해 상쇄가 된다고 지적. 예고된 초식이 절초일 때만 배제하도록 제안 | **채택 (구현 수정)** | REQ-403 은 「상대 예고가 뜨고」를 트리거로 갖는 event-driven 요구라 그 절의 모든 조건이 그 수의 예고를 기준으로 평가되는 것이 정본 독법이고, 역파는 절초가 실제로 예고된 수에만 성립하므로 그 밖의 배제는 이득 0·완파 손실만 남는다. 수정 후 B 게이트가 8수 HP 정확히 0 → 7수 HP −2·제자 92 로 여유가 늘었다 |
| 09-01 03:1x | 프로토타입 v2 구현 ② | Claude Code (Opus 5, 무인 워커) | 12수 도달 시 잔여 HP 비교를 `core.resolveMatch()` 로 승격하면서 **절대값 비교 vs 비율 비교** 결정 (유닛 ① 인계 사항 1 — spec 미지정) | **채택 (절대값)** | spec 문면이 「잔여 HP 비교」이고, 비율은 최대 HP 가 낮은 도전자를 구조적으로 유리하게 만들어 REQ-506 이 지키려는 첫 파견 승리를 뒤집는다 (제자 100 vs B 80 에서 40 대 35 가 비율로는 패). 하네스가 두 갈래를 모두 단정으로 고정 |
| 09-01 03:2x | 프로토타입 v2 구현 ② | Claude Code (Opus 5, 무인 워커) | 이슈 § 파일 스코프의 조건부 옵션 — `index.html` 이 400~500줄을 넘으면 `src/ui/` 로 화면 단위 분리 | **채택 (분리)** | 4화면 + 입력부 + 대련 루프가 한 파일에서 1400줄대로 계산돼 리뷰 가능 크기를 넘었다. 순수 로직은 유닛 ① 모듈에 그대로 두고 DOM 을 아는 코드만 `src/ui/` 로 내렸다 — `src/` 루트의 DOM-free 계약은 그대로 유지된다 |
| 09-01 03:3x | 프로토타입 v2 구현 ② | Claude Code (Opus 5, 무인 워커) 셀프 플레이 | 헤드리스 사이클 시뮬 중 **막다른 길** 발견: A-3 격파 후 차수가 4로 넘어가 대련 진입이 막히는데, 그 시점에 12성 미달이면 남은 성 성장 경로가 통째로 닫힌다 | **채택 (수정)** | 최고 차수를 클램프해 재진입을 허용 — REQ-503 의 「재진입은 도장에서 수동」과 정합하고, 스펙에 없던 제약을 구현이 만들어낸 자리였다. 함께 채택: 숙련 30% 졸업 시 **빈 슬롯 자동 장착** (선택이 없는 자리라 탭 하나가 kill (a) 60초 예산에 직결) |
| 09-01 03:4x | 프로토타입 v2 구현 ② | Claude Code (Opus 5, 무인 워커) L4 실브라우저 | 응수 창 밖에서 입력 패드를 언마운트하니 매 수(약 1.5s 주기) 레이아웃이 튀는 것이 스크린샷으로 관측됨 | **채택 (수정)** | 패드는 상시 마운트하고 창 밖에서는 흐림 + 입력 비수락으로만 바꿨다 — REQ-101 의 「엄지 존 고정」은 버튼이 매 수 자리를 잃지 않는 것까지가 요건이다 |
| 09-01 04:0x | CI 집행 층 | Claude Code (Opus 5, 무인 워커) | `scripts/comment-lint.py` 의 `.mjs` 확장에서 **정규식 리터럴 판별**을 넣을지 — 넣지 않으면 `/https:\/\//` 의 `\/` 가 줄 주석 시작으로 읽혀 그 줄 나머지를 통째로 주석으로 삼킨다 | **채택 (선행 토큰 휴리스틱)** | 현재 트리에 정규식 리터럴은 0건이지만, 이 lint 는 required check 라 오탐 1건이 이후 모든 PR 을 막는다. 선행 토큰(구두점 집합 + 키워드 집합)으로 나눗셈과 가르고, 줄 안에서 닫히지 않으면 나눗셈으로 되돌리는 보수 폴백을 뒀다. `${...}` 안의 주석은 추출을 포기(오탐 0 쪽으로) |
| 09-01 04:3x | CI 집행 층 | Claude Code `codex` L5d (적대 리뷰) | 3라운드 지적: 「제어문 헤드 판별이 **문자열 안의 괄호**를 구문 괄호로 세므로 `if ("(") /[//]x.py:12/.test(y)` 에서 여전히 오독한다 → 토큰 인식형 파서로 교체하라」 | **미채택 (기각)** | L5d 계약의 기각 트립와이어 (i)(iii) 에 걸린다 — 합성 케이스(문자열 안 불균형 괄호 + 문자 클래스 안 `//` 가 한 줄에 동시 성립)로 휴리스틱을 완전 파서로 격상시키는 발산 요구이고, 변경 전 상태(`.mjs` 집행률 0)보다 이미 보수적이다. 오탐이 실제로 나면 `lint: allow-block(<이유>)` 트레일링 주석이 그 줄만 면제하므로 required check 영구 차단 경로도 없다. 같은 라운드의 medium(diff 실패 fail-open 이 required check 를 green 으로 만든다)은 **채택** — 워크플로에서 리포트 헤더 부재를 실패로 승격 |
| 09-01 05:0x | 프로토타입 v2 구현 ③ | Claude Code (Opus 5, 무인 워커) | 인계 2 — `tests/harness.mjs` 의 `simulateDispatch` 가 `src/ui/match.mjs` 의 교차 루프를 두 번째로 구현한 문제. 대안 ⓐ 하네스 사본 유지 + 대조 단정 추가 ⓑ `createMatch` 에 프레임 시계를 주입하고 가상 시계로 실루프를 돌린다 | **채택 (ⓑ)** | ⓐ 는 사본이 두 벌인 상태를 유지한 채 세 번째 대조군을 늘릴 뿐이다. `timer = {now, schedule, cancel}` 주입은 브라우저 기본값을 그대로 두면서 `match.mjs` 의 전역 의존을 한 곳(`FRAME_TIMER`)으로 모으고, B 밸런스 게이트가 파견 화면과 **같은 코드**를 검증하게 만든다. 결과 동일(7수·제자 HP 92)이라 회귀 없음 |
| 09-01 05:1x | 프로토타입 v2 구현 ③ | Claude Code (Opus 5, 무인 워커) | 봇 v2 가 숙련 100% 초식에서 원터치(REQ-109)를 쓸지. 시도해 보니 창의 78% 가 원터치가 되고 kill (b) 표본이 30건 → 5건으로 붕괴 | **미채택 (봇은 원터치를 쓰지 않는다)** | 원터치 창은 kill (b) 분모에서 제외되므로(REQ-302·603), 탭하는 봇은 자기가 재려던 완주율 표본을 스스로 지운다. 봇의 목적은 페이스 회귀이지 최적 플레이가 아니다. `oneTap` 비율은 보조 지표로 여전히 산출 가능하고 사람 표본이 채운다 |
| 09-01 05:2x | 프로토타입 v2 구현 ③ | Claude Code (Opus 5, 무인 워커) | spec 미지정분 — REQ-605 의 봇 시드는 「15% 미입력」뿐이라 봇이 `ignore`·`reset` 을 한 번도 방출하지 않고, 그러면 REQ-601 의 17종 전량 emit 과 선행 게이트(`ignore_rate`)가 둘 다 공허해진다 | **채택 (`BALANCE.bot.misHitRate` 0.06 신설)** | 「미입력」은 키를 안 누르는 것(→ `timeout`, (b) 분모)이고 「헛손질」은 옆 버튼을 눌러 후보 0이 되는 것(→ `ignore`)이라 **다른 축**이다. 어느 후보의 다음 키도 아닌 방향만 고르므로 반드시 `ignore` 로 남아 계획이 깨지지 않고, 40시드 실측 `ignore_rate` 2.8~11.3% 로 15% 게이트를 실제로 시험한다 |
| 09-01 05:3x | 프로토타입 v2 구현 ③ | Claude Code (Opus 5, 무인 워커) | 판독기(`tests/kill-readout.mjs`)의 종료 코드를 무엇에 걸지 — kill 4항 pass/fail 인가, 판독 가능성인가 | **채택 (판독 가능성)** | kill 미달을 red 로 만들면 CI 가 「밸런스가 아직 안 맞는다」를 빌드 실패로 보고하게 되고, 판정일 전 튜닝 구간이 통째로 red 가 된다. 종료 코드는 **필드 결손·스키마 위반·17종 미방출**만 잡고 (a)(b)(d) 는 출력으로 남긴다 — 수용 케이스 11 이 요구하는 것도 「산출 가능」이지 「임계 통과」가 아니다 |
| 09-01 05:4x | 프로토타입 v2 구현 ③ | Claude Code (Opus 5, 무인 워커) | 세션 로그의 `t_ms` 출처를 `Date.now()` 로 둘지 `performance.now()` 로 바꿀지 | **채택 (단조 시계)** | kill (a)·(d) 는 경과 시간이라 시스템 시계 변경에 흔들리면 안 되고, 대련 루프·힌트 점등이 이미 `performance.now()` 를 쓰므로 로그만 다른 시계를 보면 두 축을 대조할 수 없다. 같은 주입 지점을 헤드리스 봇이 가상 시계로 쓴다 |
| 09-01 05:5x | CI import 해석 검사 | Claude Code (Opus 5, 무인 워커) | named export 파손까지 잡으려면 모듈을 실제로 해석해야 하는데, `src/ui/**` 12종은 최상위에서 DOM 을 만져 Node 에서 `import()` 가 죽는다. 후보 3안 — ⓐ 정규식 정적 파싱 ⓑ `document` 전역 스텁을 깔고 실제 `import()` ⓒ `vm.SourceTextModule.link()` 로 링크만 하고 평가는 생략 | **채택 (ⓒ)** | ⓐ 는 자체 파서를 들여야 하고 주석·문자열에서 오탐이 난다. ⓑ 는 모듈 최상위 부작용(타이머·오디오)이 그대로 돌아 required check 에 무관한 red·행을 만든다 — 차단 게이트의 오탐 1건은 이후 모든 PR 을 막는다. ⓒ 는 V8 의 ResolveExport 가 링크 단계에서 돌아 named export 파손이 잡히고 평가는 `evaluate()` 에서만 일어나 DOM 이 실행되지 않는다(node 22·26 실측). 표준 라이브러리만 쓰므로 의존성 0 계약도 지킨다 |
| 09-01 10:5x | 온보딩 (#15) | Claude Code (Opus 5, 무인 워커) | kill (d) 321.0s 미달의 원인이 조작이 아니라 온보딩 부재(도장 체류 144.2s)로 판독된 뒤, 안내 수단으로 ⓐ 별도 튜토리얼 화면·오버레이 시퀀스 ⓑ 기존 도장 화면 위 상태 기반 툴팁 | **채택 (ⓑ)** | ⓐ 는 1사이클 구조(도장→수련→대련→전수→파견→결과)에 7번째 상태를 붙여 `cycle` 이벤트 스키마와 kill 판독기의 페이즈 가정을 함께 건드린다. 문제는 「화면이 없다」가 아니라 「지금 눌러야 할 게 뭔지 안 보인다」라 표면적이 훨씬 작은 ⓑ 가 원인에 가깝다. 임계(300.0s)·`cycle_done_t` 정의는 손대지 않는다 — 미달 뒤 기준을 옮기는 것은 명시 기각 |
| 09-01 10:5x | 온보딩 (#15) | Claude Code (Opus 5, 무인 워커) | 안내 대상 판정을 ⓐ DOM 결합으로 두고 L4 로 내릴지 ⓑ `session.mjs` 의 순수 함수로 분리해 하네스 단정을 붙일지 (이슈 § 검증 L2 가 갈라 둔 자리) | **채택 (ⓑ 분리)** | 판정 입력이 `disabled` 불리언 목록뿐이라 DOM 없이 성립한다. 분리하면 「최초 진입 1건 · 신규 활성화 고지 · 재고지 안 함」 3축이 회귀망 안으로 들어오고, 실브라우저에서만 확인 가능한 것은 위치·가독성으로 좁혀진다. 대가는 `src/ui/session.mjs` 를 건드리는 것인데 순수 데이터(불리언 목록·id 문자열)만 들어가 DOM-free 계약은 유지된다 |
| 09-01 10:5x | 온보딩 (#15) | Claude Code (Opus 5, 무인 워커) | 안내 1회성의 기준 — ⓐ 버튼을 누른 순간 소비 ⓑ 화면에 띄운 순간 소비 | **채택 (ⓑ)** | ⓐ 는 장착·해제로 잠금이 오가면 같은 말풍선이 반복돼 수용 기준 3 의 「같은 조건으로 다시 뜨지 않는다」를 깬다. ⓑ 로 두되 떠 있는 안내 자체는 대상이 눌리거나 다시 잠길 때까지 유지해, 다른 화면을 한 번 다녀왔다고 유도가 사라지지 않게 했다 |
| 09-01 11:3x | hook 배선 단일화 (#11) | Claude Code (Opus 5, 무인 워커) | 공유 계측 팩토리의 거처 — ⓐ `src/ui/session.mjs` 존치(이슈가 「유력 거처」로 지목, 이미 `logVerdict`·`recordDuelVerdict` 등 공용 로깅 담지자) ⓑ 신규 `src/ui/wiring.mjs` | **채택 (ⓑ 신규 모듈)** | 이슈의 「유력 거처」 근거는 `#15` 랜딩 전 실측이다. 랜딩 후 `session.mjs` 는 277줄·export 34종이고 로깅 + 온보딩 툴팁 상태의 혼합 담지자가 됐다 — 세 번째 축(hook 배선)을 얹으면 「무엇이든 공용이면 여기」가 되어 이 이슈가 고치려는 것과 같은 종류의 응집 상실을 만든다. 별 모듈이면 DOM-free 계약도 파일 단위로 자명해지고, `check-imports` 가 그 간선을 따라 named export 파손까지 본다 |
| 09-01 11:3x | hook 배선 단일화 (#11) | Claude Code (Opus 5, 무인 워커) | 미결 설계 판단 — DOM 렌더 hook 과 계측 hook 의 합성 방식: ⓐ 팩토리가 렌더 콜백을 받아 안에서 부른다 ⓑ 화면이 팩토리가 준 계측 위에 자기 렌더를 얹는다 | **채택 (ⓑ 화면이 얹는다)** | ⓐ 는 렌더 콜백의 타입·수명이 팩토리 시그니처에 새겨져 헤드리스가 쓰지 않는 인자를 계속 넘겨야 하고, 그 압력이 결국 DOM 을 팩토리 안으로 끌어들인다 — 그러면 하네스가 import 하지 못해 공유 자체가 무너진다. `composeHooks(계측, 얹을 것)` 은 같은 이름을 **덮어쓰기가 아니라 연결**로 처리해 화면이 계측 hook 을 조용히 잃는 경로를 구조적으로 없앤다. 계측 반환값(`changes`·`ranked`·실행된 초식)을 두 번째 인자로 넘겨 화면이 재계산하지 않게 했다 |
| 09-01 11:3x | hook 배선 단일화 (#11) | Claude Code (Opus 5, 무인 워커) | 수련 배선을 대련·파견과 같은 `createMatch` hook 형태로 맞출지 | **미채택 (형태를 맞추지 않음)** | 수련에는 `createMatch` 가 없고 창을 여는 지점과 완주 지점 둘뿐이다. `onTelegraph`/`onTick` 을 흉내 내면 아무도 부르지 않는 hook 이 생겨 「hook 이름 집합」 회귀 pin 이 실제 배선이 아니라 형식을 지키게 된다. `onArm`(창 길이를 돌려준다)·`onFire` 2종으로 두고 하네스가 그 집합을 그대로 단정한다 |
| 09-01 13:1x | 화면 고정 (#24) | Claude Code (Opus 5, 무인 워커) | 이슈 § 결정 사항이 축자로 지정한 `--k: min(calc(100vw / 393), calc(100dvh / 852), 1)` 을 그대로 심을지 | **수정 채택 (`min(tan(atan2(100vw, 393px)), tan(atan2(100dvh, 852px)), 1)`)** | 지정 문면은 `min()` 안에 길이 2개와 무단위 수 1개를 섞어 타입이 어긋난다 — 헤드리스 Chrome 152 실측에서 `transform: none` 으로 통째로 떨어졌다(축소 자체가 무발화). `tan(atan2(<길이>, <길이>))` 가 같은 비율을 무단위 수로 돌려주고 상한 1도 같은 `min()` 안에 남는다. 「CSS 만으로 도출」이라는 결정의 실질은 그대로다 |
| 09-01 13:1x | 화면 고정 (#24) | Claude Code (Opus 5, 무인 워커) | 바깥 래퍼의 중앙 정렬을 이슈 문면대로 `place-items: center` 로 둘지 | **수정 채택 (`place-content: center`)** | 스테이지의 레이아웃 상자(852px)가 창보다 클 때 `place-items` 는 그리드 **행 안에서** 정렬하므로 행 자체가 시작점에 붙어 스테이지 아래가 잘린다 — 실측 360x640(Chrome 152)에서 하단 106px 소실. `place-content` 는 행을 컨테이너 안에서 중앙에 놓아 5개 뷰포트 전부 상하·좌우 여백이 같아진다 |
| 09-01 13:1x | 화면 고정 (#24) | Claude Code (Opus 5, 무인 워커) | `#pad` 의 `env(safe-area-inset-bottom)` 처분 — 이슈 § 배경의 원인 4종에 없던 다섯 번째 세로 변동 항 | **채택 (제거)** | 기기마다 값이 달라지는 세로 항이라 「패드 높이 상수」와 직접 충돌한다. 게다가 스테이지가 레터박스 중앙에 놓이면 패드 하단이 더는 기기 가장자리에 접하지 않아 값의 의미 자체가 성립하지 않는다. 대가는 창이 정확히 9:19.5 인 실기기에서 하단 홈 인디케이터와 겹칠 여지이고, 그 확인은 L4 앵커 이슈로 넘긴다 |
| 09-01 13:0x | 툴팁 오버레이 전환 (#20) | Claude Code (Opus 5, 무인 워커) | 오버레이 말풍선의 기준 상자 — ⓐ 앵커(`.tip-anchor`)를 유일한 기준 상자로 두고 좁은 폭 넘침은 `max-width` 상한으로 막는다 ⓑ 가로로 선 줄에서는 `.row-actions` 를 기준 상자로 삼아 줄 전체 폭을 물려받는다 | **채택 (ⓑ)** | ⓐ 는 앵커 폭이 곧 버튼 폭이라(가로 줄의 「수련」은 약 52px) 말풍선이 열 줄로 접히고, 그것을 피하려면 `max-width` 에 앵커의 줄 내 x 오프셋을 CSS 가 모르는 채로 어림한 상수를 박아야 한다. ⓑ 는 `left:0; right:0` 만으로 폭이 정확히 줄 폭이 되어 수용 기준 3(좁은 폭 미넘침)이 상수 없이 성립한다. 앵커는 여전히 흐름 밖 말풍선을 담을 뿐이라 크기 불변 — 대형 버튼 카드 쪽은 앵커 자신이 기준 상자로 남는다. 함께 처분: `#15` 가 넣은 `align-items: flex-start` 2건은 「앵커가 툴팁만큼 커진다」가 전제라 전제 소멸과 함께 삭제 |
| 09-01 19:2x | 도장 세로 예산 (#37) | Claude Code (Opus 5, 무인 워커) | 바닥 밴드의 구현 형태 — ⓐ `#pad` 를 화면마다 다른 내용으로 재사용 ⓑ `#screen` 과 `#pad` 사이에 빈 `#band` 슬롯을 두고 화면이 채운다 | **채택 (ⓑ)** | ⓐ 는 패드가 입력기(`createPad`)의 소유물이고 그 DOM 구조(방향키·후보·버퍼)가 고정이라, 도장 액션을 넣으려면 패드가 화면 종류를 알아야 한다 — 입력 경로 단일화(REQ-101)가 깨진다. ⓑ 는 `#band:empty { display: none }` 로 「채우지 않은 화면에는 자리도 없다」가 CSS 한 줄로 성립하고, `go()` 가 전환마다 비우므로 화면이 남긴 밴드가 다음 화면에 새지 않는다. 함께 처분: `#pad.detached` 를 `visibility: hidden`(272px 상시 점유) → `display: none` — 「전환마다 높이가 튄다」던 예약 사유가 현재 코드에는 없다. 수련의 재무장 `attach` 는 이미 붙은 패드라 `.detached` 토글이 없고, 대련 종료의 `detach` 는 `go('result')` 와 같은 태스크 안이라 그 사이에 페인트 경계가 없다 |
| 09-01 19:2x | 도장 세로 예산 (#37) | Claude Code (Opus 5, 무인 워커) | 아코디언 기본 펼침 우선순위 — 이슈 § 4 는 「유도 툴팁 대상 행」만 지정. 사용자가 다른 행을 편 뒤 툴팁 대상이 옮겨가면 무엇이 이기는가 | **수정 채택 (사용자 선택 > 툴팁 대상 > 첫 장착 슬롯)** | 툴팁이 항상 이기게 두면 안내가 떠 있는 동안 사용자가 다른 초식의 시퀀스를 볼 수 없다. 선택을 그때의 안내 대상 **행**에 묶어 두면 두 요구가 충돌하지 않는다 — 안내가 다른 초식 줄로 옮겨가는 순간 선택이 만료돼 그 뒤 진입마다 지목된 행이 열린다(선택을 무기한 남기면 한 번 접은 뒤로 안내 행이 영영 접힌 채 뜬다). 나머지 수용 기준(대상 버튼이 접힘 아래로 안 내려간다)은 우선순위가 아니라 구조로 지켰다 — 수련·장착·배우기 버튼을 줄 헤더로 올려 접힘 여부와 무관하게 보인다 |
| 09-01 19:3x | 도장 세로 예산 (#37) | Claude Code (Opus 5, 무인 워커) 실브라우저 | `#20` 이 정한 말풍선 기준 상자(`.row-actions`)가 액션 줄 소멸로 사라진 뒤, 후속 상자를 `.row-head` 로 둘지 `.row` 로 둘지 | **채택 (`.row`)** | `.row-head` 는 스크린샷 실측에서 말풍선이 바로 아래 시퀀스 화살표를 통째로 덮었다 — 화살표는 입력 기억 장치라 안내가 그것을 가리면 안내의 대상이 안 보인다. `.row` 는 `#20` 이 만든 배치(말풍선이 줄 아래에 뜬다)를 그대로 잇고 `left:0; right:0` 만으로 폭이 줄 폭이 되는 성질도 유지한다. 밴드 쪽은 화면 최하단이라 반대로 위(`bottom: calc(100% + 4px)`)로 띄웠다 |
| 09-01 19:5x | 도장 세로 예산 (#37) | Claude Code `codex` L5d (적대 리뷰) | medium 지적: 안내가 밴드 버튼을 가리키거나 소진돼 `guidedRow` 가 `null` 인 동안에는 아코디언 선택이 만료되지 않아 대련·수련을 다녀와도 그 행이 그대로 열려 있다 → 도장 이탈 시 선택을 리셋하고 브라우저 수준 회귀 단정을 붙여라 | **부분 채택 (문서만 정정, 리셋·테스트는 기각)** | 「안내가 같은 행에 머무는 동안 사용자 선택이 유지된다」는 이 변경의 의도다 — 수련하러 갔다 온 사이 사용자가 편 행이 닫히면 아코디언이 선택 장치로서 무의미해진다. 안내가 초식 줄을 가리키지 않는 국면에는 「툴팁 대상 행」 자체가 없어 수용 기준이 공허하게 참이고, 사용자는 탭 1회로 어느 행이든 다시 연다. 실제로 틀린 것은 코드가 아니라 `README` 문장(「진입 시 …가 펼쳐져 있다」)이라 그것을 기본값 서술로 정정했다. 브라우저 단정은 이 repo 의 하네스가 DOM-free 계약이라 신설 층이 필요하고, 지적 등급(medium)이 그 비용을 정당화하지 않는다 |
| 09-01 20:0x | 성 적립 게이트 (#38) | Claude Code (Opus 5, 무인 워커) | 적립 게이트를 어디에 둘지 — ⓐ `applyEffectiveSuccess` 의 적립분만 막는다 ⓑ 적립 + **성 조회**를 함께 막는다 | **채택 (ⓑ)** | ⓐ 만으로도 실제 경로에서는 포인트가 0 이라 성이 1 이지만, 수용 기준 3(「12성은 전 초식 100% ∧ pt ≥ 30 일 때만」)이 코드로 참이 되지 않는다 — 포인트가 어떤 경위로든 채워지면 12성이 열린다. `rankPtsOf()` 가 입문 전에는 0 을 답하게 해 성 조회의 단일 경로를 게이트에 통과시켰고, 그래야 하네스 단정 (c) 가 공허하지 않다. `rankForPts` 는 순수 계단으로 남겨 제자 축(게이트 예외)이 그대로 쓴다 |
| 09-01 20:1x | 성 적립 게이트 (#38) | Claude Code (Opus 5, 무인 워커) | 이슈에 없던 막다른 길 — 슬롯 3 · 초식 4 라 4식이 영원히 장착되지 못해 **전 초식 100% 가 도달 불가**했다 (자동 장착이 빈 슬롯만 채운다). ⓐ 봇만 교체 판단을 갖는다 ⓑ 도장 자동 장착이 입문 전 한정으로 자리를 양보한다 | **채택 (ⓑ)** | ⓐ 는 사람 플레이어를 막힌 상태로 남긴다 — 「해제」 버튼은 유도 툴팁 대상이 아니라(안내할 이유가 없는 버튼으로 명시 제외) 무엇을 눌러야 하는지 화면 어디에도 없다. D1 이 전 초식 100% 를 **필수 경로**로 만든 이상 그 구간의 장착은 선택이 아니므로, 자동 양보가 REQ-305 의 「4 > 3 선택」을 없애는 게 아니라 **선택이 실제로 생기는 시점을 입문 뒤로 옮긴다**. 부수 효과로 입문 시점 슬롯이 강·정·쾌를 덮는 2·3·4식이 되어 D3 가 가정한 평균 3pt 가 실제로 성립한다 |
| 09-01 20:2x | 성 적립 게이트 (#38) | Claude Code (Opus 5, 무인 워커) | 사이클 시뮬의 「유효 성공률 70%/50%」 축을 무엇으로 만들지 — ⓐ 봇에 원터치를 열어 D4 가 말하는 손 속도 보상을 모델한다 ⓑ 손 정확도(`missRate`) 시드만 갈아 끼운다 | **채택 (ⓑ)** | ⓐ 를 실제로 붙여 보니 1식이 100% 가 된 뒤 완파가 매 수 이어지고, 완파→빈틈이 예고 회전 인덱스의 짝수 자리를 계속 먹어 2식의 예고(β)가 영영 오지 않는 **교착**이 났다 (600 화면 상한 초과). 이것은 개정 규칙의 결함이 아니라 「완파 연환 + 짝수 회전」이 만드는 기존 데이터의 잠재 성질이고, 봇이 창을 자주 흘려서 가려져 있었다. 봇이 원터치를 쓰지 않는다는 계약은 kill (b) 표본 보호가 근거라(09-01 05:1x) 시뮬 편의로 뒤집을 자리가 아니어서 되돌렸다. 대가는 시뮬 수치가 **사람보다 보수적**이라는 것이고, 그 편차를 balance-log `0-e` ④ 에 명시했다 |
| 09-01 20:3x | 성 적립 게이트 (#38) | Claude Code (Opus 5, 무인 워커) | D3 파라미터 시드표(수련 3 · `threshold` 3·3·4·4 · `rankStep` 2 · A HP 45/60/110)를 그대로 확정할지 | **미채택 (시뮬 산출값으로 대체)** | 시드표는 72% 시나리오에서 `cycle_done` 481s 로 kill (d) 300s 를 크게 깬다. 이슈 R1 이 지목한 실패 형태 그대로라, 12시드 × 2 시나리오 스윕으로 `threshold` 2·2·2·2 · 수련 2 · `rankStep` 2 · A HP 30/45/80 을 산출해 확정했다 (72% 285s 통과 / 47% 395s 초과 — 초과분은 balance-log `0-e` ③ 에 남은 축과 함께 기록). `rankStep` 1(12성 15pt)이 47% 시나리오를 더 줄이지만 1pt/성 계단에서 4식 한 방이 4성을 올려 D1 이 기각한 「다단 승급 난사」를 그대로 부르므로 쓰지 않았다 |
| 09-02 04:3x | 성 축 초식 단위 재설계 (#64) | Claude Code (Opus 5, 무인 워커) | 성 계단 비용·도전자 성 시드의 **시뮬 산출 방법** (이슈가 구현 세션에 위임) — ⓐ 기존 `0-e` 사이클 시뮬(하네스 § 14-a)을 성 축 이정표로 확장 ⓑ 계단 전용 스윕 스크립트 신설 | **채택 (ⓐ 확장)** | 판단 기준은 「하네스에서 재현 가능할 것」 하나였고, § 14-a 는 이미 12시드 × 정확도 시나리오 2종을 결정적으로 돌려 그 조건을 만족한다. 신설하면 봇 손 모델·가상 시계·로그 판독을 두 벌로 들고 그중 하나가 조용히 낡는다. 이정표만 성 축으로 갈아 끼웠다 — 입문·성포인트 → 해금(5성)·원터치(7성)·결정타 계단·12성·전수. **kill (d) 300s 는 목적함수의 제약으로 넣지 않았다** (운영자 판정 기준 지시 ①): 관측치로만 `balance-log` `0-f` 에 기록했고, 사이클이 3~4배로 늘어난 것을 이유로 계단 비용을 깎지 않았다 |
| 09-02 04:4x | 성 축 초식 단위 재설계 (#64) | Claude Code (Opus 5, 무인 워커) | 치트 패널의 **마운트 지점** (이슈가 구현 세션에 위임) — ⓐ `index.html` 의 비모듈 `<script>` 에 인라인 ⓑ `src/ui/` 신규 모듈 | **채택 (ⓑ `src/ui/cheat.mjs`)** | 판정 기준은 DOM-free 계약이다. 하네스가 import 하는 8모듈(`src/` 루트 4 + `src/ui/` 4)에 들어가지 않으면 되고, 신규 `src/ui/` 모듈은 그 집합 밖이라 계약을 그대로 지킨다. ⓐ 는 비모듈 스크립트라 `session.mjs` 의 변경 함수를 import 할 수 없어 주입이 `window.__dojo` 를 통한 **두 번째 쓰기 경로**가 되고, 그 경로는 어떤 스키마도 테스트도 지키지 않는다. 주입·플래그·로그 자체는 DOM-free 인 `session.mjs` 에 두어 하네스가 REQ-782·783 을 직접 단정한다 — `index.html` 은 마운트 지점(`#cheat`·`#cheatBtn`)만 진다 |
| 09-02 04:4x | 성 축 초식 단위 재설계 (#64) | Claude Code (Opus 5, 무인 워커) | 봇이 11·12성 계단에서 멈춘다 — 결정타·완파는 「이기는 색」 선택으로는 특정 초식에 떨어지지 않는다 (실측: 시드 7919 가 4000화면에서 미완주, 유운보 10성 고착). ⓐ 봇에 계단을 미는 손을 준다 ⓑ 계단 조건을 완화한다 | **채택 (ⓐ)** | ⓑ 는 운영자 판정 기준 지시 ①(예산을 이유로 한 규칙 완화 불채택)에 정면으로 걸린다. 그리고 이것은 규칙의 결함이 아니라 **봇에 없던 사람의 선택**이다 — 사람은 10성 초식이 있으면 그 초식을 골라 친다. `preferLadderPush` 는 결정타 계단(10성)이면 항상, 완파 계단(11성)이면 파해 대상이 예고됐거나 상대 빈틈일 때만 민다(그 수에만 완파가 성립하므로). 대가는 완주율(b) 하락이고 `balance-log` `0-f` ③ 에 명시했다 |
| 09-02 04:5x | 성 축 초식 단위 재설계 (#64) | Claude Code (Opus 5, 무인 워커) | `sv: 2` 판별 토큰의 부착 층 (spec § build-Plan 미결) — ⓐ `log.mjs` 스키마 상수에 선언하고 적재 층이 붙인다 ⓑ 호출부가 이벤트마다 명시로 넘긴다 | **채택 (ⓐ)** | ⓑ 는 넘기는 자리를 두는 만큼 잊는 자리가 생기고, 그 누락은 로그가 조용히 구 스키마로 읽히는 형태라 검증이 어렵다. ⓐ 는 `LOG_SCHEMA[event].sv` 하나가 정본이라 「어느 이벤트가 뜻이 바뀌었는가」가 표에서 읽히고, `validate()` 는 여전히 호출부 필드만 대조하므로 스키마 밖 필드 검사도 그대로 산다. 판독기 쪽은 `t_ms` 와 같은 자리에서 토큰을 벗기고 값만 대조한다 |
| 09-02 05:0x | 성 축 초식 단위 재설계 (#64) | Claude Code (Opus 5, 무인 워커) | `rev` ↔ 값 결합 강제 방식 (#54 흡수분) — ⓐ 값 지문을 계산해 `rev` 에 묶는다 ⓑ 커밋 훅으로 `rev` 변경을 검사한다 | **채택 (ⓐ 값 지문)** | ⓑ 는 이 repo 의 검증층(`node --check`·하네스·lint) 밖에 새 층을 만들고, 훅을 안 건 머신·CI 에서 무발화다. ⓐ 는 로더가 이미 도는 자리라 브라우저·헤드리스·CI 전부에서 같은 시점에 죽고, 오류 문면이 **그대로 붙여 넣을 새 `rev` 를 준다**(「<판본>/<지문> 으로 갱신하라」)라 튜너의 마찰이 1회 복사로 끝난다. FNV-1a 32bit + 키 정렬 직렬화라 의존성 0 계약도 지킨다 |
| 09-02 05:1x | 성 축 초식 단위 재설계 (#64) | Claude Code `code-reviewer` L5 (Blocker) | 「한 수 최대 1계단」(REQ-704)이 세션 합성 경로에서 뚫린다 — `recordDuelVerdict` 가 적립 뒤에 계단을 판정해, 9성 + 잔여 적립 상태의 결정타가 9→10(적립)→11(결정타)로 두 계단을 올린다 (실측 재현: `rank` 로그 `9->10:duel` + `10->11:finish`) | **채택 (계단을 적립보다 먼저 판정)** | 두 판정은 그 수의 **같은 성**을 봐야 한다. 계단은 적립이 멈춘 성(10·11)에서만 열리므로 계단을 먼저 두면 둘 중 하나만 발화하는 것이 구조로 보장된다 — 계단이 열리면 그 뒤 적립은 `ladderBandAt(11|12) === null` 로 무효고, 계단이 안 열리면 적립이 정상 동작한다. 하네스가 green 이었던 이유는 계단 케이스가 `promoteByOutcome` 단독과 「이미 10성·pts 0」 합성만 훑었기 때문이라, 9성 + 잔여 적립 합성 단정을 세션 경로로 박았다 |
| 09-02 05:1x | 성 축 초식 단위 재설계 (#64) | Claude Code `code-reviewer` L5 (Important) | 빈틈 완파가 12성 조건을 충족한다 — `gradeOf` 는 `foeOpen` 이면 무조건 `crush` 라, 11성 초식이 파해 관계와 무관하게 빈틈 한 수로 12성이 된다. spec § REQ-731 은 「파운현월의 완파 무대 = A-4」를 전제로 재대련 상한을 정당화한다 | **미채택 (현행 유지 · 후속 유닛으로 이관)** | ⓐ `gradeOf` 는 이슈 #64 가 **무변경**으로 못박은 함수다 — 완파 정의를 파해 완파로 좁히는 것은 6단 판정표 자체의 변경이고 골든값이 움직인다 ⓑ REQ-702 의 12성 조건 문면은 「그 초식으로 완파 1회」로 완파의 종류를 가르지 않는다 ⓒ REQ-731 은 후속 유닛(#65) 소관이라 그 전제를 여기서 손대면 스코프 침범이다. **다만 지적 자체는 유효하다** — #65 가 재대련 상한을 세우기 전에 「빈틈 완파를 12성 자격으로 인정하는가」를 먼저 정해야 하고, 인정하면 REQ-731 의 정당화 문면을 고쳐야 한다. 이 결정은 그 판정을 #65 로 넘긴다는 것이지 「문제 없음」이 아니다 |
| 09-02 05:2x | 성 축 초식 단위 재설계 (#64) | Claude Code `code-reviewer` L5 (Important) | `cheat_flagged` 가 내보내기에는 실리는데 `tests/kill-readout.mjs` 가 그 키를 읽지 않아 주입 세션이 정상 표본으로 통과한다 (REQ-782 미충족) | **채택 (판독 자체를 막는다)** | 이슈 § 수용 기준은 「로그 + 세션 플래그」까지만 적었지만 spec 수용 기준 케이스 14 는 「플래그 세션이 kill 판독 스크립트에서 제외됨」을 요구한다 — **spec 이 정본**이라 spec 을 따랐다. 제외를 「지표에서 빼기」가 아니라 「판독 실패」로 구현한 것은, 주입 세션의 (b)(c)(d) 가 전부 오염되므로 남길 유효 지표가 없기 때문이다. `log_violations` 실패 경로와 같은 자리에 두어 대칭을 지켰다 |
| 09-02 05:1x | 성 축 초식 단위 재설계 (#64) | Claude Code `codex` L5d (S1 review, P1) | 결정타 판정이 `outcome.over && outcome.win` 이라 **수 상한(12수) 잔여 HP 비교승**까지 결정타로 세어, 그 수의 초식이 11성으로 오른다 | **채택 (`by === 'hp'` 로 한정)** | spec REQ-704·용어 사전이 결정타를 「**승리를 확정한** 마지막 타격」으로 정의한다. 수 상한 판정승은 그 타격이 승리를 확정한 것이 아니라 시계가 끝나고 HP 를 비교한 결과라, 같은 수를 결정타로 세면 「11성은 사건으로만 열린다」는 계단의 무게가 시간 초과로 대체된다. 하네스에 `by:'exchanges'` 승리 → 성 불변·`finish` 로그 부재 단정을 박았다 |
| 09-02 05:2x | 성 축 초식 단위 재설계 (#64) | Claude Code `codex` L5d (S2 adversarial, medium) | 치트 표본 제외가 `cheat_flagged` **봉투 키**에만 걸려, 내보내기에서 `entries` 배열만 떼어 내면 플래그 세션이 정상 표본으로 판독된다 | **채택 (`cheat` 이벤트 존재도 함께 본다)** | 플래그만 보면 「봉투를 버리는 것」이 곧 세탁 경로가 된다 — 판독기가 이미 「없는 키를 통과로 접으면 지운 파일이 가장 깨끗해 보인다」를 원칙으로 든 자리라 대칭을 맞췄다. 로그 본문의 `cheat` 이벤트는 봉투와 무관하게 남으므로 두 근거를 OR 로 묶으면 벗겨 낸 배열도 같은 판정을 받는다 (실측: `entries` 만 남긴 파일이 제외로 떨어진다) |
| 09-02 05:2x | 성 축 초식 단위 재설계 (#64) | Claude Code `codex` L5d (S2 adversarial, high) | `slot`·`transmit`·`dispatch` 가 REQ-791 의 뜻-바뀜 목록에 있는데 구 스키마(`sv` 미부여)로 남아 있다 → 세 이벤트의 필드 집합을 개정하고 `sv: 2` 를 부여하라 | **미채택 (스코프 밖 — 후속 유닛 소관)** | 세 이벤트의 **신규 필드가 이 유닛에 존재하지 않는 개념**이다: `slot{challenger}` 는 REQ-736(도전자 예고 슬롯 교체 · #65), `transmit{art, styles:[{id,rank}]}` 는 REQ-761(전수 개정 · #66), `dispatch{stage, foe_set, locked_until}` 는 REQ-741·742(파견 2단화 · #66). 여기서 부여하면 `stage`·`locked_until` 같은 값을 지어내야 한다. 이 유닛의 REQ 범위(701~708·711~715·721~723·781~783)와 이슈 § 파일 스코프가 `log.mjs` 변경을 `mastery`/`initiate` 제거 + `rank`/`unlock` sv:2 + `finish`/`rank_wall`/`cheat` 신설로 명시 열거했고, 세 이벤트는 이 유닛에서 **뜻이 바뀌지 않았다**(발화 조건·필드 그대로). 판독기가 그것을 「현행 스키마」로 받는 것도 정확하다 — 후속 유닛이 필드를 바꾸는 그 커밋에서 `sv: 2` 를 함께 붙이는 것이 REQ-791 의 취지다 |
| 09-02 05:4x | 사부 대련 확장 (#65) | Claude Code (Opus 5, 무인 워커) | 역파 `decayPerRank` 시드 0.05 를 그대로 확정할지 (이슈가 구현 세션에 위임) | **미채택 (0.075 로 교체)** | 시드 0.05 는 **관통 하한 0.4 를 도달 불가로 만든다** — 하한이 물리려면 성 차 12 가 필요한데 역파가 실재하는 두 무대의 정의역 상계가 8 이다 (사부 12성 vs A-4 초회 성 4 · 제자 10성 vs B 성 2, 우연이 아니라 양쪽이 같다). 하한이 사표(死表)면 REQ-771 의 「절초는 무서워야 한다」를 지키는 장치가 아무것도 없고 수용 기준의 경계값 검증도 공허하게 참이 된다. 0.075 는 하한이 **정확히 그 상계에서** 물려 「가장 여문 초식이 맞을 때가 최대 감쇠」가 되고, 0.1 은 성 차 6 에서 이미 하한이라 성장 보상 구간을 잘라낸다. 하한 0.4 는 이슈·수용 기준이 40% 로 명시했으므로 고정하고 계수만 움직였다 — 하한을 난이도 손잡이로 쓰지 않는다는 REQ-771 의 단일 목적과도 같은 방향이다 |
| 09-02 05:4x | 사부 대련 확장 (#65) | Claude Code (Opus 5, 무인 워커) | A-4 를 넣자 헤드리스 1사이클이 데드락했다 (실측: 시드 20260901 이 6000화면 미완주, 적운압정 10성 고착 · 완파 31/2874수). 원인은 봇이 **최고 차수만 반복**하는데 적운압정의 파해 대상 β 가 A-4 예고에 없다는 것 — ⓐ 봇에 무대 선택을 준다 ⓑ A-4 구성에 β 를 넣는다 | **채택 (ⓐ `nextDuelStage`)** | ⓑ 는 REQ-731 이 축자로 정한 A-4 구성(α·γ·δ)을 바꾸는 것이라 스코프 밖이고, 「파해를 하나씩 가르치는 학습 계단」도 무너진다. 데드락은 규칙의 결함이 아니라 **봇에 없던 사람의 선택**이다 — 사람은 적운압정을 12성으로 만들려면 β 를 내는 도전자에게 돌아간다. 무대는 데이터에서 파생한다(그 초식의 파해 대상을 예고에 가진 최저 해금 차수). 함께 개정: `preferLadderPush` 가 결정타 계단을 **조건 없이** 밀던 것을 완파 성립 수로 한정했다 — 조건 없이 밀면 계단에 선 초식 하나가 매 창을 독점해 나머지 초식의 적립이 굶고(실측: 피격 1101/2874), 결정타는 어차피 그 창의 최대 피해가 최선이라 조건이 같다. 부수 효과로 사이클이 짧아졌다 (`balance-log` `0-g`) |
| 09-02 05:5x | 사부 대련 확장 (#65) | Claude Code (Opus 5, 무인 워커) | 「도전자 예고 화면」(REQ-732·736)의 거처 — ⓐ 대련 중 매 수의 예고(`telegraphView`)에 절초 공개·슬롯 교체를 얹는다 ⓑ 대련 진입 전 예고 화면을 신설한다 (파견 쪽 `renderPreview` 의 사부 대련 대응물) | **채택 (ⓑ 신설 화면 · 라우트 `duelPreview`)** | ⓐ 는 REQ-736 이 성립하지 않는다 — 응수 창이 도는 중에 슬롯을 바꾸는 것은 판단이 아니라 조작 사고이고, 「판단의 순간과 조작의 장소를 붙인다」는 결정의 취지는 **창이 열리기 전**을 가리킨다. 용어 사전도 도전자 예고를 「대련 전 상대 유파·절초를 공개하는 준비 단계」로 정의한다. 도장 「대련」·「재대련」 버튼이 이 화면을 거치게 하고 봇·헤드리스 사이클도 같은 경로를 지나게 했다 — 봇만 건너뛰면 재대련·슬롯 계측이 화면에서만 나온다 |
| 09-02 05:5x | 사부 대련 확장 (#65) | Claude Code (Opus 5, 무인 워커) · #64 L5 이관 판정 | 빈틈 완파를 12성 자격으로 인정하는가 (#64 가 「#65 가 재대련 상한을 세우기 전에 먼저 정해야 한다」며 이관) | **인정 (현행 유지) · 전제 문면 정정** | spec REQ-702·704 는 「그 초식으로 낸 완파 1회」로 완파의 종류를 가르지 않고, 판정표 SoT 도 빈틈 수를 「완파 취급」으로 정의한다. 자격을 파해 완파로 좁히면 「완파인데 12성이 아닌 완파」가 생겨 도장 눈금의 「완파 필요」 표식이 거짓이 되고, `gradeOf` 무변경 계약과도 부딪친다. **대신 전제를 정정한다** — 이슈 § 리스크와 REQ-731 의 「파운현월 완파는 A-4 에서만 가능」은 **파해 완파에 한정된 서술**이다 (실측: 11성 파운현월이 빈틈 수에 완주하면 어느 차수에서든 12성이 열린다). 따라서 재대련 상한 +3 은 「유일 경로의 영구 봉쇄 방지」가 아니라 **주 경로의 도달 가능성 보전**이고, 그래도 필요하다 — 무한 누적은 δ 역파의 유일한 사부 무대이자 파운현월의 유일한 *파해* 완파 무대를 이길 수 없게 만든다. 하네스에 빈틈 완파 자격을 명시 단정으로 박아 다음 회차가 이것을 결함으로 오독하지 않게 했다 |
| 09-02 06:0x | 사부 대련 확장 (#65) | Claude Code (Opus 5, 무인 워커) | `judge` 의 상대 축을 어떻게 넓힐지 — 역파 감쇠에는 상대 **성**이 필요한데 현행 인자는 내공(`foePower`)뿐이다. ⓐ `foePower` 에서 성을 역산 ⓑ `foeRank` 를 필수 인자로 더하고 `foePower` 기본값을 그 파생으로 둔다 | **채택 (ⓑ)** | ⓐ 는 `powerOf` 의 역함수를 부동소수에서 반올림해야 하고, 그 반올림이 한 계단 틀리면 감쇠 계수가 조용히 어긋난다. ⓑ 는 기본값이 파생이라 두 값이 갈릴 자리가 구조적으로 없고, 결손은 `selfRank` 와 같은 형태로 즉시 throw 한다 — 「감쇠가 NaN 으로 새어 그 수에서야 죽는」 것이 이 축의 실패 모드다. 골든값에는 두 값을 함께 명시해 감쇠와 무관한 수의 기대값이 #64 판본 그대로임을 눈으로 대조할 수 있게 남겼다 |
| 09-02 06:0x | 사부 대련 확장 (#65) | Claude Code (Opus 5, 무인 워커) | `rematch{attempt_n}` 을 언제 찍을지 — ⓐ 대련 **진입** 시점 ⓑ 대련 **결과** 시점 | **채택 (ⓐ 진입)** | 지표가 「재대련이 몇 번째에 멈추는지」(REQ-734·794)라, 결과로 거르면 **마지막 회차 — 대개 포기한 그 판 — 가 통째로 빠져** 분포의 오른쪽 끝이 잘린다. 진입 기준이면 항목 하나가 곧 「한 번 더 쳤다」이고 그 최댓값이 중단 지점이다. `attempt_n` 은 그 도전자와의 대면 서수(초회 1 · 재대련부터 2·3…)이고 초회에는 이벤트를 남기지 않는다 — 초회는 재대련이 아니기 때문이다. 패배 뒤 재도전은 승수를 올리지 않아 같은 서수로 다시 찍히는데, 그것도 「한 번 더 쳤다」라 지표의 뜻에 맞다 |
| 09-02 06:2x | 사부 대련 확장 (#65) | Claude Code `codex` L5d (S2 adversarial, high) | 시드 0~499 중 6건(99·153·155·318·387·496)이 헤드리스 1사이클을 화면 상한 안에 끝내지 못한다 — 유운보가 8성에 고착한 채 A-4 를 무한 반복. 리뷰어 처방은 「10성 미만 초식이 있으면 그 초식을 진전시키는 무대를 고르라」(`nextDuelStage` 확장) | **수정 채택 (무대가 아니라 손의 동률 규칙을 고쳤다)** | 재현 결과 원인은 무대가 아니었다 — 유운보는 슬롯에 있었고 A-4 에는 그 파해 대상 α 가 매 3수 온다. 실제 원인은 **「이기는 색」 동률의 tie-break 이 슬롯 순**이라는 것이다: A-4 예고의 강(α)에 우세인 후보가 쾌 둘(유운보·파운현월)인데 앞 슬롯이 창을 전부 가져가 뒤 초식이 적립에서 굶었다(발동 1082회 중 유운보 11회 실측). 무대를 더 고르게 해도 그 창 안에서 같은 초식이 다시 선택되므로 처방이 원인에 닿지 않는다. 동률을 **덜 여문 초식 우선**으로 뒤집자(`growthOrder`) 500/500 완주 · 사이클 1028s→862s. 리뷰어가 지목한 6시드를 그대로 회귀 스위트로 박고, 판별자를 「완주 여부」가 아니라 **낸 초식의 분포**로 두었다 — 완주만 보면 다음 굶주림이 상한 밑에서 조용히 지나간다 |
| 09-02 06:4x | 제자 축 확장 (#66) | Claude Code (Opus 5, 무인 워커) | 제자 수련 타이머의 **영속화** (이슈가 구현 세션에 위임) — ⓐ `localStorage` 로 세션 밖까지 잇는다 ⓑ 세션 내 타이머만 둔다 | **채택 (ⓑ 세션 내 타이머)** | 코드 정찰 결과 이 repo 에는 승계할 저장소 경로가 **하나도 없다** — `session.mjs` 헤더가 「영속화는 없다: 프로토 판정은 단일 세션이고, 잔존 세이브는 kill (a)·(d) 측정을 진행 중 상태로 오염시킨다」로 그 부재를 이미 계약으로 적어 두었다. 세이브를 새로 들이면 그 계약을 뒤집는 결정이 되고, 그것은 이 유닛의 스코프가 아니다. M1 판정이 단일 세션이라 세션 내 타이머로 요구가 성립하며, 방치의 **길이**(1성당 1800s)는 그대로 두고 재방문 대신 주입 시계(하네스·1시간 수련 시뮬)로 관측한다 |
| 09-02 06:4x | 제자 축 확장 (#66) | Claude Code (Opus 5, 무인 워커) | `sv: 2` 판별 토큰의 **부착 층** (이슈가 구현 세션에 위임) — 신설 5종(`rank_wall`·`rematch`·`finish`·`disciple_train`·`cheat`)에 붙으면 「구 스키마가 있다」는 거짓 신호가 된다 | **현행 유지 (이벤트별 선언 · 신설 5종 무부착)** | 코드 정찰 결과 #64 가 이미 그 형태로 지었다 — `LOG_SCHEMA[event].sv` 가 **이벤트별 선언**이고 적재 층은 선언이 있을 때만 붙인다(`if (sv !== undefined)`). 층이 일괄 부착하는 형태였다면 신설 5종이 함께 물들었겠지만, 선언형이라 이 유닛이 할 일은 뜻이 바뀐 `transmit`·`dispatch` 에 선언 두 줄을 더하는 것뿐이다. 새로 짓지 않은 만큼 하네스에 **부재 단정**을 박았다 — 신설 5종의 `sv` 가 `undefined` 임을 이름으로 확인해, 다음 회차가 층을 일괄 부착형으로 바꾸면 그 자리에서 red 가 된다 |
| 09-02 06:5x | 제자 축 확장 (#66) | Claude Code (Opus 5, 무인 워커) | `dispatch` 로그를 **언제 찍을지** — 신 스키마가 `result` 를 지므로 진입 시점(`logDispatchStart`)에는 값이 없다. ⓐ 진입에 찍고 결과를 뒤에 덧쓴다 ⓑ 종료 시점으로 옮긴다 | **채택 (ⓑ 종료 시점 · `logDispatchResult`)** | ⓐ 는 로그 항목을 사후 변형하는 경로를 여는데, 이 버퍼는 append-only 이고 그 성질이 판독 계약의 바탕이다(내보낸 JSON 이 곧 사실). 구간의 **시작점**은 `cycle{phase:'dispatch'}` 가 이미 지므로 진입 시점 항목이 따로 없어도 판독에 구멍이 나지 않는다. `rematch` 를 진입에 찍은 것과 방향이 반대로 보이지만 축이 다르다 — `rematch.attempt_n` 은 「몇 번째에 그만두는가」라 결과로 거르면 마지막 회차가 빠지고, `dispatch` 는 승패 자체가 필드다 |
| 09-02 06:5x | 제자 축 확장 (#66) | Claude Code (Opus 5, 무인 워커) | B-2 이후 임무의 **도전자 표현** — ⓐ `CHALLENGERS` 에 임무 행을 차수마다 신설 ⓑ 파견 무대(`B`) 하나를 유지하고 조합·성만 주입 | **채택 (ⓑ 무대 1개 + 주입)** | ⓐ 는 `BALANCE.hp`·`challengerRank` 가 `CHALLENGERS` 와 1:1 이어야 한다는 로더 검증(`checkContentJoins`)에 걸려 차수마다 데이터 두 줄이 늘고, 차수가 무제한이라 그 표가 끝나지 않는다. ⓑ 는 `createMatch` 가 이미 `foeRank` 주입을 받으므로(재대련이 쓰는 그 자리) 난이도 곡선이 데이터 신설 없이 성립하고, HP 는 파견 무대 하나를 공유한다 — spec REQ-742 도 임무 난이도를 **성**으로만 올린다고 적었다. 임무 종류·보스는 M2 자리로 그대로 남는다 |
| 09-02 06:5x | 제자 축 확장 (#66) | Claude Code (Opus 5, 무인 워커) | REQ-753 의 「`simTrainSeconds`·`simEfficiency` 를 제자 수련 계산에 연결」을 어떻게 읽을지 — ⓐ 방치 압축 버튼이 제자 수련 시계를 그만큼 앞당긴다 ⓑ `simEfficiency` 를 수련 진척의 효율 계수로 곱한다 | **채택 (ⓐ 시계 앞당김)** | ⓑ 는 0.1 이 **재화/초** 단위라 진척에 곱하면 1성당 5시간이 되어 방치 루프를 되레 늘린다 — 단위가 다른 값을 재사용하는 형태다. ⓐ 는 그 버튼의 원래 목적(REQ-604 「평가자는 실제로 방치할 수 없으므로 1시간을 버튼 하나로 압축해 보여 준다」)을 제자 축으로 그대로 넓히는 것이고, **게임 수치(1성당 1800s)를 건드리지 않는다** — 콘텐츠 삭감의 반대 방향이다(REQ-792) |
| 09-02 15:1x | 화면 UI·아트 재설계 — sfx 6종 (#80) | Claude Code (Opus 5, 절차적 DSP 합성) | 아트 계약의 sfx 6행을 외부 생성 도구(Suno/ElevenLabs)로 뽑을지, 절차적 합성으로 직접 만들지 | **채택 (절차적 합성 — sfx 한정, bgm 제외)** | 결정 요인은 품질이 아니라 **요구사항 적합성과 재현성**이다. spec 이 `sfx_key` 에 요구하는 「재생 시 피치가 랜덤화되므로 원본은 무색·무잔향」(REQ-922)은 생성 모델이 구조적으로 어긋나는 요구다 — 음악적으로 듣기 좋은 결과 = 피치가 뚜렷한 결과이기 때문이다. 합성은 그 요구를 파라미터로 직접 강제할 수 있고(부분음을 비조화 비율 1:1.47 로 배치), 시드 고정이라 밸런스 조정 때 「조금 더 건조하게」가 가능하다. Suno 산출물은 재유도 불가능한 블랙박스라 같은 조정이 불가능하다. 산출물의 SoT 를 오디오가 아니라 `sfx_recipes.json` 으로 둬 `.ogg` 를 빌드 프로덕트로 강등한 것도 같은 이유다. **`bgm_dojo` 는 제외** — 「기억에 남지 않는 무협 긴장감 루프」는 DSP 프리미티브의 영역이 아니라 외부 생성을 유지한다 |
| 09-02 15:2x | 화면 UI·아트 재설계 — sfx 6종 (#80) | Claude Code (Opus 5) 자체 검증 | 재렌더 시 바이트 동일성이 깨짐 — 「결정론 렌더러」주장의 반증 | **수정 채택 (ffmpeg bitexact + serial 고정)** | 원인은 합성 코드가 아니라 **Ogg 컨테이너**였다 — 매 인코드마다 랜덤 스트림 serial 과 인코더 문자열이 심긴다. 격리 실험(동일 입력 2회 인코드)으로 코덱이 아닌 컨테이너 층임을 확인한 뒤 `-fflags +bitexact -flags +bitexact -map_metadata -1 -serial_offset 1` 로 고정했다. 이 검증을 건너뛰었다면 레시피 SoT 구조 전체가 근거 없는 주장으로 남았을 것이다 — 결정론은 선언이 아니라 측정으로만 성립한다 |
| 09-02 19:4x | 화면 UI·아트 재설계 — 횡단 용어 개정 (#94) | Claude Code (Opus 5, 무인 워커) | 기성 초식 4종의 **창안자 값**을 무엇으로 확정할지 — spec REQ-891 이 「값은 구현 단계에서 확정한다」로 위임했고, 목업의 「운객(雲客)」은 플레이스홀더로 명시돼 있었다 | **채택 (초식마다 다른 선대 1인 — 운허자 雲虛子 · 중산객 重山客 · 석계자 石溪子 · 월현옹 月玄翁)** | 네 초식에 같은 창안자를 넣으면 필드가 무공 속성의 중복이 되어 「초식마다 창안자를 갖는다」(D-4)가 데이터에서 성립하지 않고, 창안 시스템이 붙었을 때 사부·유저 이름이 들어갈 자리의 다양성도 미리 죽는다. 이름은 각 초식의 구결에서 뽑았다 — 흘려보내는 유운보에 雲虛, 무게로 누르는 적운압정에 重山, 흐름을 읽는 행운유수에 石溪, 달을 드러내는 파운현월에 月玄. **폰트 파급을 감수한 선택이다** — 한자 8자가 서브셋 밖이라 커버리지 게이트(REQ-804)가 red 가 되고 `subset-fonts.sh` 재생성을 동반한다. 반대 방향(현행 41자 안에서만 이름을 짓는다)은 서브셋이 콘텐츠를 결정하게 하는 것이라, 게이트가 그러라고 있는 것이 아니다 |
| 09-02 22:4x | 화면 UI·아트 재설계 — S3 수련·S4 파견 이식 (#97) | Claude Code (Opus 5, 무인 워커) | `selectDiscipleStyle` 의 `reason` **3계열이 선택 갈래 5개를 덮지 못한다** — 코드의 갈래는 빈틈(위력 최대) · 우세 · 상쇄 · 잔여(열세) · 역파 회피 다섯인데 spec REQ-853 이 고정한 계열은 우세/상쇄/역파 회피 셋뿐이다. ⓐ 계열을 5종으로 늘린다 ⓑ 셋으로 접고 접힌 갈래를 문서로만 설명한다 ⓒ 셋으로 접되 접힘이 화면에서 거짓말이 되지 않음을 **단정으로 세운다** | **채택 (ⓒ — 3계열 유지 + `assertAttrCoverage`)** | ⓐ 는 spec 이 고정한 값을 구현이 늘리는 것이라 스코프 밖이고, 관전 문구가 계열마다 하나씩 필요해 spec 미기재 문구를 구현이 지어내게 된다. ⓑ 는 **잔여(열세) 갈래가 「상쇄를 골랐다」로 표시되는 거짓말**을 산문으로 덮는 것이라 repo 주석 정책이 금지하는 형태다. ⓒ 의 근거: 접힘 둘의 성질이 서로 다르다 — **빈틈**은 상대 색이 없고 어떤 완주든 완파라 「우세를 골랐다」가 참이고, **잔여**는 참이 아니므로 *화면에 도달하지 않음*을 코드로 보장해야 한다. 제자는 무공을 통째로 물려받으므로(`discipleStyles`) 그 무공이 세 속성을 다 덮으면 어떤 예고에도 우세나 상쇄가 반드시 있다 — 이것을 `assertAttrCoverage(STYLES)` 부팅 단정으로 세웠다. 잔여 갈래는 후보가 이미 좁혀진 **대련 봇**(`chooseStyle`)에서만 닿고 그쪽은 `reason` 을 읽지 않는다. 역파 회피는 **배제가 실제로 선택을 바꿨을 때만** 발화하게 했다 — 어차피 고르지 않았을 초식을 뺀 것을 「피했다」로 부르면 δ 예고마다 그 문구가 뜬다(실측: δ 는 fine, 그 파해 대상 유운보는 fast 라 애초에 우세 후보가 아니다) |
| 09-02 22:4x | 화면 UI·아트 재설계 — S3 수련·S4 파견 이식 (#97) | Claude Code (Opus 5, 무인 워커) | 파견이 상속할 **아레나 세간(기력 2·예고·응수 창)의 소유자** — #96 이 연 `arena.mjs` 는 「무대 높이·서는 자리·자세」 셋만 갈아 끼우는 계약면이고 세간은 대련 화면이 직접 지었다. ⓐ 파견도 자기 세간을 짓는다 ⓑ 세간을 `arena.mjs` 로 올려 두 화면이 상속한다 | **채택 (ⓑ — `bout` 인자 신설)** | ⓐ 는 기력 바 좌표·예고 스트립 조판·응수 창 게이지가 두 화면에 두 벌 생기는 것이라, REQ-850 의 「전부 같은 좌표」가 **선언으로만 남고 다음 튜닝에서 갈린다**. #96 이 계약면을 연 이유가 정확히 그 포크를 막는 것이었으므로 첫 상속자가 포크를 만들면 그 유닛의 존재 이유가 사라진다. ⓑ 는 REQ-850 이 열거한 그 세 가지를 인자 하나(`bout` = 기력 두 줄의 라벨)로 묶어, 실전이 벌어지지 않는 화면(S6 결과)에는 `setVital`·`showTelegraph`·`setWindow` 가 **존재하지 않게** 했다 — 있지도 않은 세간을 조용히 no-op 으로 만드는 대신 오타가 그 자리에서 TypeError 로 죽는다. 기력 자리 이름을 `foe`/`self` 에서 `far`/`near`(= `SPOT`)로 뒤집은 것도 같은 축이다: 파견의 근경은 사부가 아니라 제자라 `self` 가 거짓이 된다 |
| 09-02 22:4x | 화면 UI·아트 재설계 — S3 수련·S4 파견 이식 (#97) | Claude Code (Opus 5, 무인 워커) | REQ-841 「구결 3구절이 방향 한 개씩에 1:1 대응」의 **데이터 표현** — 현행 `gugyeol` 은 산문 한 줄이고 구절 경계가 데이터에 없다. ⓐ 렌더 시점에 공백으로 쪼갠다 ⓑ 구절 배열을 따로 신설하고 산문은 그대로 둔다 ⓒ `gugyeol` 자체를 구절 배열로 바꾼다 | **채택 (ⓒ)** | ⓐ 는 초식마다 어절 수가 달라(3~8) 시퀀스 길이로 나누는 규칙이 없고, 나눠지더라도 「달을」 같은 조각이 구결이 아니게 된다. ⓑ 는 같은 문면이 두 필드에 앉아 한쪽만 고치는 자리를 만든다. ⓒ 는 산문이 필요한 자리(죽간 툴팁)가 `join(' ')` 하나라 소비처 비용이 0 이고, 구절 수와 시퀀스 길이의 결합을 `assertGugyeol` 부팅 단정으로 세울 수 있다 — 그 결합이 어긋나면 **점등이 시퀀스와 다른 것을 가리키는데** 화면에는 에러가 뜨지 않는 것이 이 축의 실패 모드다 |
