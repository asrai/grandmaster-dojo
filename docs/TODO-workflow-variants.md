# TODO — workflow variants (HTML5)

`/gamedev-init` (2026-08-31) recorded `engine: HTML5 (vanilla JS)`. The dotfiles workflow templates ship engine variants only for Godot / Unity (`ci.godot.yml`, `ci.unity.yml`, gitleaks configs, seeds), so this repo received the language-agnostic set only (`claude.yml`, `claude-review.yml`, `agent-usage-report.yml`, `risk-triage.yml`, PR/issue templates, ADR template, SECURITY.md, gitleaks pre-commit hook, comment-lint).

Still to author by hand:

- [x] `.github/workflows/ci.yml` — single `ci` job: `node --check` over every git-tracked `*.mjs`, `node tests/harness.mjs` judged by exit code, `scripts/comment-lint.py` self-test + full-corpus advisory scan + ratchet run (issue #6). The `main protection` ruleset already existed with 4 other rules but no `required_status_checks` rule (`post-create-hardening.sh` omitted it because no stack was detected), so context `ci` is attached to that ruleset as the post-landing step of the same session (a required check cannot name a job that is not yet on `main`); the re-query evidence is in issue #6. The job carries **no path filter** on purpose — a filtered PR would never report `ci` and would be blocked forever.
- [x] Import-resolution check — `scripts/check-imports.mjs`, run as a step of the same `ci` job (issue #9). `node --check` parses each module in isolation, so a broken relative `import` path or a missing named export still passed; nothing else in CI loaded `src/ui/**` (the harness imports only the DOM-free modules), leaving a broken `index.html` entry chain invisible until manual playtest. The checker links every git-tracked `*.mjs` as an entry through `vm.SourceTextModule.link()`, which runs V8's ResolveExport but not evaluation — so the DOM-touching modules are covered without a browser; the `index.html` module-script `src=` edge is checked as well, since a broken graph and a broken entry edge are both invisible to `node --check`. It is a step, not a new job, because a new job name would have to be re-registered in the `main protection` ruleset.
- [ ] `.gitleaks.toml` — no HTML5 variant exists; the default gitleaks ruleset is used by `scripts/githooks/pre-commit` until a repo-specific allowlist is needed (issue #1 tracks the fallback).
- [ ] Human-speed bot (Chrome) as a manual-dispatch workflow (`workflow_dispatch`) once the v2 input model lands — it needs a browser runner, so it stays off the per-PR path. This is the **CI-side** dispatch and is a different axis from the developer-local driver `scripts/l4-serve.sh` + `scripts/l4-sweep.sh` (issue #231), which already exists: the local pair owns process cleanup on a developer machine, where a leaked headless Chrome survives the session as a PPID=1 orphan. A hosted runner is torn down with the job, so the workflow does not need the sweep — but if the bot is ever wired to a self-hosted runner, it must drive the page through `l4-serve.sh` for the same reason a local session does.
- [ ] `scripts/check-imports.mjs` target glob — it links git-tracked `*.mjs` only, so the first `.js`/`.cjs` file to land is skipped silently: the checker still exits 0 and its report reads exactly like a pass, which is how a broken relative path or a missing named export in that file would reach `main` unseen (carried over from issue #9 step 5e ⓓ). Widen the glob, or make an unmatched JS-family extension an explicit report line, when such a file first appears.
- [ ] `.editorconfig` — the templates README lists it but no template file exists in `workflow-templates/`; add a minimal one (2-space, LF, UTF-8) when convenient.
